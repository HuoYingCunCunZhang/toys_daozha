# 道闸玩具 · 需求规格说明书 v2.1

> 状态：需求已冻结，可进入 CAD / 电路 / 固件设计
> 上游：`道闸玩具_电路设计方案_v1.md`（那是实现方案，不是需求）
> 下游：`道闸玩具_电路与结构设计方案_v2.md`

---

## 1. 产品定义

一个**能听懂话的电动道闸玩具**。孩子对着它说"起杆"，闸杆抬到竖直；说"落杆"，闸杆落下挡住小汽车；小车顶到闸杆时，闸杆会自己弹回去，不会硬压。

- **目标玩家**：6-10 岁儿童
- **形态**：桌面 / 地面摆件，配一个独立小遥控器
- **制造**：结构件 3D 打印（嘉立创三维打印），电路 = 嘉立创打样的**模块承载底板** + 现成模块

---

## 2. 功能需求

### 2.1 核心：语音命令（仅 3 条）

| ID | 说法（可挂多个同义词） | 动作 |
|---|---|---|
| 1 | 起杆 / 抬杆 / 开门 | 闸杆抬到 **90° 竖直** |
| 2 | 落杆 / 降杆 / 关门 | 闸杆落到水平 |
| 3 | 复位 | 从故障态恢复，回到落杆待机 |

- 本地离线识别，**不联网、不上云**
- 唤醒方式：用乐鑫现成唤醒词（避开付费定制），或按机身按键作为"唤醒"后 5 秒内说命令
- **明确不做**：语音播报、TTS 合成、录音回放（→ 因此整机不装喇叭和功放）

### 2.2 手动控制

- 底座顶面 **仅 2 个按键：抬杆 / 落杆**，位于底座**长脚一侧**（与闸杆伸出方向相反，杆不会扫过按键）
- **复位** 有三个入口：语音说"复位" / 遥控器复位键 / 底座两键**同时长按 3 秒**
- 独立无线遥控器，3 个按键（抬杆 / 落杆 / 复位），ESP-NOW 直连，不需要路由器

### 2.3 防砸

- **不用测距传感器**。~~电机堵转检测（0.33Ω 采样电阻 + ADC）~~ → **已推翻**，改用**行程超时反转**（见下方⚠）
- 落杆超过标定行程时间的 1.15 倍仍未到位（= 顶到东西）→ **反向抬杆 + 蜂鸣报警**；超 1.5 倍 → FAULT 断电
- 优点：覆盖 300mm 闸杆**全长**（侧装测距传感器做不到），零额外模块，成本 ≈ ¥0.5
- 闸杆仅 9.4g、N20 扭矩很小，物理挤压风险本身就极低

> ⚠️ **本条已从毫秒级降级为秒级，需要确认是否接受。**
> 原方案（0.33Ω 采样电阻 + ADC，80ms 内反转）**在成品 DRV8833 模块上做不到**：H 桥电机电流从 `AISEN` 脚回地、不经过 `GND` 脚，而成品模块没把 `AISEN` 引出来，把电阻串在 `GND` 上只能读到芯片几 mA 的静态电流，防砸会完全失效。详见 [网表基准 v2 §5.1](道闸玩具_底板网表基准_v2.md)。
> 改用行程超时后的**实际响应延迟**：行程刚开始受阻 ≈1.6s，中段 ≈0.9s，接近落位 ≈0.2s。这期间闸杆压在障碍物上的力约 **1.3N（≈130 克）**。
> 若要恢复毫秒级：需换成**引出 AISEN 的模块**或直接用裸 DRV8833 芯片自己布板。

### 2.4 反馈

- **蜂鸣器**：命令确认"嘀"一声，故障时连续短鸣（唯一的反馈通道）
- **不装爆闪灯、不装闸杆灯带**
- ⚠️ 取消爆闪灯后整机**没有任何视觉状态提示**，见 §6 遗留项

### 2.5 电源

- 内置锂电池，**Type-C 充电口在底座前端面**（长脚远端，远离道路侧）
- 总电源拨动开关，同在前端面
- 低电量由蜂鸣器提示

### 2.6 状态机

`BOOT → HOMING → CLOSED ⇄ OPENING/CLOSING ⇄ OPEN`，任意态可进 `FAULT`（电机断电、蜂鸣、按"复位"恢复）。

---

## 3. 结构尺寸需求（已冻结）

**坐标约定**：原点在底座底面中心，**-X = 前（长脚侧，按键 + Type-C）**，**+X = 闸杆伸出方向（道路侧）**，Y = 高度，Z = 车辆行驶方向。

| 部位 | 尺寸 | 说明 |
|---|---|---|
| **底座** | 长 150(X) × 宽 55(Z) × 高 **20**(Y) mm | X 从 -75 到 +75 |
| **闸体（黄）** | 46 × 46 × 高 62mm，X = +2~+48 | 顶面离地 **82mm** |
| **闸杆** | 长 **300** × 宽 **5** × 高 **8**mm | 从闸体沿 **+X** 伸出，红白相间 |
| **转轴中心** | X=+25，**Y=42**，轴线沿 Z，转毂从闸体 Z=-23 侧壁伸出 | |
| **闸杆离地** | 杆底 38mm，**杆顶 46mm** | **见 §6-1：由小车高度反推，不再是 80mm** |
| **起杆角度** | **90°（完全竖直）** | 凸轮限位 + 机械止挡双重定位 |
| **按键** | **2 个**，底座顶面。~~X = -50 / -30~~ → 实际位置由 PCB 定，现为 **(0, +20) 和 (+20, +20)**，φ9 孔 | 位置随 PCB 布局走，见 [机械约束 §七之二](道闸玩具_底板机械约束_v1.md) |
| **Type-C + 总电源开关** | 底座**前端面** X = -75 | 远离道路侧，插线不挡车 |
| **爆闪灯 / 天窗** | **取消** | |
| **测距传感器 / 防砸使能开关** | **取消** | 改用行程超时反转（原定的堵转电流检测已推翻，见 §2.3） |
| **遥控器** | 60 × 30 × 20mm，Type-C 充电，3 键 | |

---

## 4. 非功能需求

**安全（6-10 岁）**
- 电池必须带过充/过放/短路保护
- 落杆末端缓停，不能"啪"地砸下
- 行程超时反转（1.15×T 反转 / 1.5×T 进 FAULT）
- 所有小件（配重块、限位件）必须螺丝或结构固定，**不允许可脱落的可吞咽小件**

**体验**
- 按键/遥控 → 动作启动 < 200ms
- 语音命令说完 → 动作启动 < 800ms
- 一次完整起杆/落杆 1.3-1.7s（90° 行程，模拟真实道闸节奏）
- 连续待机监听 ≥ 8 小时（实测预算 ≈15h）

**可制造**
- 闸杆 300mm 做**两段拼接**，兼容 220mm 幅面打印机
- 电路 = 一块承载底板 + 现成模块插接，可手工焊接、可单独更换

---

## 5. 明确排除项

| 砍掉的 | 原因 |
|---|---|
| 喇叭 + MAX98357A | 不做语音播报。副作用是好的：没喇叭就没有"自己唤醒自己"的声学难题 |
| 离线 TTS / 录音回放 | 同上 |
| 闸杆 WS2812 灯带 | 闸杆只有 5mm 宽，灯带 10mm 装不上 |
| 顶部爆闪灯 + 天窗 + 半透光片 | 用户取消 |
| **VL53L0X 测距 ×2** | 用户可选取消。且侧装传感器无法覆盖 300mm 闸杆全长，堵转检测是更好的方案 |
| 防砸使能拨动开关 | 随测距传感器一起取消 |
| 底座复位按键 | 用户改为 2 键；复位走语音 / 遥控器 / 双键长按 |
| 18650 电池 | 底座只有 20mm 高，装不下 |
| 常开模式（HOLD_OPEN） | 语音命令收敛到 3 条，无对应入口 |

---

## 6. 遗留项

1. **闸杆高度已按小车反推**：用户小车高 50~100mm，取最矮的 50mm 为约束 → 杆顶必须低于 50mm。定为杆顶 **46mm**（余量 4mm），确保 50mm 的车也挡得住；100mm 高的车在起杆（90° 竖直）时可正常通过。
2. **无视觉反馈**：取消爆闪灯后只剩蜂鸣器。若后续想补，最小代价方案是在两个按键下方各加一颗贴片 LED（按键帽用半透料），不改外形、不加凸起。
3. **90° 竖直的过冲风险**：闸杆到 90° 时重力矩为零，若限位失效会越过竖直向后倒。已在闸体上设计**机械止挡筋**作为第二道保险。
4. **ToF 可随时加回**：VL53L0X 约 ¥9/只，两只 ¥18，成本并不高。取消的真正理由是覆盖范围，不是价格。若后续想做"来车自动抬杆"，GPIO 7/8/15/16 已预留。
