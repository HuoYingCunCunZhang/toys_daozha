# ⚠️ 本文档已过时

> 已被 [底板网表基准 v2](道闸玩具_底板网表基准_v2.md) 取代，**保留仅供追溯**。
> 原理图是按 v2 连线并校验的，不要照本文接线。

---

# 道闸玩具 · 模块承载底板 网表基准 v1

> 依据：`道闸玩具_电路与结构设计方案_v2.md`（v2.1）
> **用途**：这是画原理图/PCB 之前的"正确性基准"。画完之后我会用 EDA API 读回实际网表，与本文件逐条比对。
> **请先审这份，确认电路本身对，我再动手画。**

---

## 1. 板子定位

一块 **承载底板**：现成模块用排母插上去，只有电源防护元件、采样电阻、按键、端子焊在板上。
外形受底座内腔限制：**≤ 146 × 51 mm，元件高度 ≤ 13 mm**（底座内高 16mm，DevKit 插排母后约 13.2mm）。

---

## 2. 元件位号表

### 2.1 插接模块（排母，不焊死）

| 位号 | 器件 | 封装/连接 | 说明 |
|---|---|---|---|
| U1 | ESP32-S3-DevKitC-1 N16R8 | 2 × 1×22P 排母，行距 22.86mm | 主控，**平放** |
| U2 | DRV8833 双路电机驱动模块 | 1×10P 或 2×6P 排母（按实物） | 只用 A 通道 |
| U3 | TP4056 Type-C 充电模块（带 DW01+FS8205） | 1×4P 排母 / 焊盘 | **Rprog 换 2kΩ ≈ 580mA** |
| U4 | 5V 升压模块 SX1308 / MT3608 | 1×4P 排母 | 输出调 **5.1~5.2V** |
| U5 | INMP441 I2S 麦克风模块 | 经 J3 引到闸体内 | 装在闸体前面板 |

### 2.2 板载分立元件

| 位号 | 值/型号 | 作用 |
|---|---|---|
| C1 | 470µF / 10V 低 ESR 电解 | DRV8833 VM 储能，**必须紧贴 VM 引脚** |
| C2 | 100nF 陶瓷 | DRV8833 VM 高频去耦 |
| C3 | 220µF / 10V | 主控 5V 侧"小水库"，电机拉低总线时供电 |
| C4 | 100nF | 主控 5V 去耦 |
| C5 | 100nF | 电池采样滤波 |
| C6 | 100nF | 电流采样 RC 滤波 |
| D1 | 1N5819 肖特基 | 电源隔离，压降 0.3V |
| R1 | **0.33Ω 1W** | **电机电流采样**（防砸检测核心） |
| R2 | 10kΩ | 电流采样 RC |
| R3 | 100kΩ | 电池分压上臂 |
| R4 | 100kΩ | 电池分压下臂 |
| R5 | 1kΩ | 蜂鸣器三极管基极限流 |
| Q1 | S8050 NPN（或 2N3904） | 蜂鸣器驱动。**不能直接用 GPIO 灌 30mA** |
| SW1 | 6×6×5 轻触 | 抬杆 |
| SW2 | 6×6×5 轻触 | 落杆 |
| SW3 | SS12D00 拨动 | 总电源 |

### 2.3 端子

| 位号 | 脚数 | 引到哪 |
|---|---|---|
| J1 | 2P | N20 电机 |
| J2 | 3P | 双限位微动开关（共 GND） |
| J3 | 6P | INMP441 麦克风（引到闸体） |
| J4 | 2P | 103450 锂电 |
| J5 | 2P | 有源蜂鸣器 |

---

## 3. 网表（逐条连接表）

> 记法：`位号.引脚名`。GPIO 编号即 ESP32-S3 的 IO 号。

### 3.1 电源

| # | 网络名 | 连接点 |
|---|---|---|
| N1 | `BAT+` | J4.1, U3.B+ |
| N2 | `BAT-` | J4.2, U3.B- |
| N3 | `VBAT_PROT` | U3.OUT+, SW3.1 |
| N4 | `VBAT_SW` | SW3.2, U4.IN+, R3.1 |
| N5 | `+5V` | U4.OUT+, C1.1, C2.1, D1.阳极, U2.VM |
| N6 | `5V_MCU` | D1.阴极, C3.1, C4.1, U1.5V |
| N7 | `3V3` | U1.3V3, U5.VDD, J5.1 |
| N8 | `GND` | U3.OUT-, U4.IN-, U4.OUT-, U1.GND, U5.GND, U5.L/R, C2.2, C1.2, C3.2, C4.2, C5.2, C6.2, R1.2, R4.2, SW1.2, SW2.2, J2.1, Q1.E, U2.BIN1, U2.BIN2 |

> **注意 N2 与 N8 是两个独立网络**。带保护板的 TP4056，`B-` 和 `OUT-` 之间隔着 FS8205 的 MOS，系统地必须取自 `OUT-`，接到 `B-` 会绕过过放保护。

### 3.2 电机与电流采样

| # | 网络名 | 连接点 |
|---|---|---|
| N9 | `MOT_GND` | U2.GND, R1.1, R2.1 |
| N10 | `I_SENSE` | R2.2, C6.1, **U1.GPIO7** |
| N11 | `M_AIN1` | U1.GPIO9, U2.AIN1 |
| N12 | `M_AIN2` | U1.GPIO10, U2.AIN2 |
| N13 | `M_SLP` | U1.GPIO11, U2.nSLEEP |
| N14 | `MOTOR_A` | U2.AOUT1, J1.1 |
| N15 | `MOTOR_B` | U2.AOUT2, J1.2 |

> DRV8833 的 B 通道不用：`BIN1`/`BIN2` 接 GND，`BOUT1`/`BOUT2` 悬空。`FAULT` 脚 v1 悬空（v2 可接 GPIO38）。

### 3.3 传感与人机

| # | 网络名 | 连接点 |
|---|---|---|
| N16 | `VBAT_DIV` | R3.2, R4.1, C5.1, **U1.GPIO1** |
| N17 | `LIMIT_UP` | U1.GPIO12, J2.2 |
| N18 | `LIMIT_DN` | U1.GPIO13, J2.3 |
| N19 | `BTN_UP` | U1.GPIO17, SW1.1 |
| N20 | `BTN_DN` | U1.GPIO18, SW2.1 |
| N21 | `MIC_SCK` | U1.GPIO4, J3.3 |
| N22 | `MIC_WS` | U1.GPIO5, J3.4 |
| N23 | `MIC_SD` | U1.GPIO6, J3.5 |
| N24 | `BUZ_CTL` | U1.GPIO2, R5.1 |
| N25 | `BUZ_BASE` | R5.2, Q1.B |
| N26 | `BUZ_DRV` | Q1.C, J5.2 |

J3 定义：`1=3V3, 2=GND, 3=SCK, 4=WS, 5=SD, 6=GND(L/R)`

**共 26 个网络。**

---

## 4. 几处需要你确认的设计判断

1. **蜂鸣器加了三极管驱动**（Q1+R5）。有源蜂鸣器约 20-30mA，ESP32-S3 单脚绝对最大 40mA，直驱在余量边缘。多两个元件换可靠性，我认为值。

2. **采样电阻串在 DRV8833 的 GND 回流路径**，不是串在 VM。这样 R1 一端接地，单端 ADC 直接读，不需要差分放大。代价是 DRV8833 的地被抬高最多约 264mV（800mA 堵转时），但它的逻辑高阈值只有 1.5V，ESP32 输出 3.3V，完全无影响。

3. **0.33Ω 的选值**：400mA 运行 → 132mV；800mA 堵转 → 264mV。ESP32-S3 ADC 12bit / 11dB 衰减约 0.76mV per LSB，132mV ≈ 174 个码，分辨率够用。R1 功耗最大 0.21W，选 1W 封装有余量。

4. **限位开关走 NC 常闭接法**：COM 接 GND（J2.1），NC 接 GPIO 并开内部上拉。断线 = 读到高 = 判定"已到位" = 电机立即停。这是 fail-safe，反过来接断线会让电机一直顶到冒烟。

5. **DRV8833 的 B 通道 BIN1/BIN2 接地而非悬空** —— 悬空输入会乱翻转，虽然 BOUT 没接负载但会白白耗电、发热。

---

## 5. 布线约束（画 PCB 时必须遵守）

| 约束 | 原因 |
|---|---|
| **C1(470µF) + C2(100nF) 紧贴 U2 的 VM/GND 引脚**，走线越短越粗越好 | 这是整个设计最容易翻车的地方：N20 启动电流是运行的 3~5 倍，升压输出瞬间塌陷会把主控 brownout 复位，表现为"一按遥控器整机重启" |
| **D1 + C3 构成主控的独立储能**，D1 必须在 C3 之前 | 电机拉低 +5V 时 D1 反向截止，C3 撑住主控那几毫秒 |
| **R1 的回流环路要小**：U2.GND → R1 → 系统地，环面积尽量小 | 大环路会把电机开关噪声耦进采样 |
| 电机走线（N14/N15）线宽 ≥ 1mm | 峰值 800mA |
| 星形地：所有 GND 汇到 U3.OUT- 附近一点 | 避免电机地电流流经主控地 |
| **J3（麦克风）走线远离电机走线和升压模块** | I2S 是数字信号但麦克风模拟前端怕干扰 |
| SW1/SW2 放在板子 -X 端（底座长脚侧），对齐上盖板 X=-50 / -30 的按键孔 | 结构已定死 |
| J4/U3 的 Type-C 口朝 -X 端面 | 结构已定死，Type-C 开孔在底座前端面 |

---

## 6. 与结构的接口（已在 CAD 里定死，不能改）

| 项 | 值 |
|---|---|
| 板子最大外形 | 146 × 51 mm，元件高 ≤ 13mm |
| 按键孔位 | 上盖板 X = -50 / -30，Y = 0，φ6.5 |
| Type-C 开孔 | 底座前端面 X = -75，10×5mm，离底 8mm |
| 总电源开关孔 | 底座前端面，8×4mm，Y = +19 |
| 走线孔（去闸体） | X = +25，Y = -16.5，φ8 —— **J1/J2/J3 应靠近这里** |
| 电池仓 | X = -71~-21，Y = ±17（板子这块区域要避让） |
| 主控支撑柱 | 中心 (11, 10)，63 × 25.5 |

---

## 7. 待确认

1. **DRV8833 模块的实际引脚顺序** —— 市面上至少有 1×10P、2×6P 两种排布，`nSLEEP` 有的标 `SLP` 有的标 `STBY`。**请拍一张你手上模块（或打算买的链接）的照片/丝印**，否则排母间距和网表脚名可能对不上。
2. **TP4056 模块的引脚数** —— 带保护的版本通常有 `B+ B- OUT+ OUT-` 四个焊盘，但有些板子把 OUT 标成 `+ -`。同样需要确认丝印。
3. 升压模块 SX1308 vs MT3608 的引脚是否一致（一般都是 IN+ IN- OUT+ OUT-，但确认一下）。

这三条不确认就画，PCB 打回来大概率对不上孔。
