# 道闸玩具 · 模块承载底板 网表基准 v2

> 依据：`道闸玩具_电路与结构设计方案_v2.md`（v2.1）
> **用途**：画原理图/PCB 之前的"正确性基准"。画完后用 EDA API 读回实际网表，与本文件逐条比对。
> v1 → v2：**修正一个会导致防砸失效的错误**（详见 §5.1），改用行程超时防砸，删除电流采样电路。

---

## 1. 板子定位

**承载底板**：现成模块用排母插上，只有电源防护元件、按键、端子焊在板上。
外形受底座内腔限制：**≤ 146 × 51 mm，元件高度 ≤ 13 mm**。

---

## 2. 元件位号表

### 2.1 插接模块

| 位号 | 器件 | 连接 | 说明 |
|---|---|---|---|
| U1 | ESP32-S3-DevKitC-1 N16R8 | 2 × 1×22P 排母，行距 22.86mm | 主控，**平放** |
| U2 | DRV8833 双路电机驱动模块 | 排母（按实物） | 只用 A 通道 |
| U3 | TP4056 Type-C 充电模块（带 DW01+FS8205） | 1×4P | 见 §5.3 |
| U4 | 5V 升压模块 **SX1308 或 MT3608** | 1×4P | 二者等价，见 §5.4 |
| U5 | INMP441 I2S 麦克风模块 | 经 J3 引到闸体 | 装闸体前面板 |

### 2.2 板载分立元件

| 位号 | 值/型号 | 作用 |
|---|---|---|
| C1 | 470µF / 10V 低 ESR | DRV8833 VM 储能，**必须紧贴 VM 引脚** |
| C2 | 100nF | VM 高频去耦（手册要求 VM 至少 10µF 陶瓷，模块自带 + C1/C2 已满足） |
| C3 | 220µF / 10V | 主控 5V 侧储能，电机拉低总线时顶住 |
| C4 | 100nF | 主控 5V 去耦 |
| C5 | 100nF | 电池采样滤波 |
| D1 | 1N5819 肖特基 | 电源隔离，压降约 0.3V |
| R3 | 100kΩ | 电池分压上臂 |
| R4 | 100kΩ | 电池分压下臂 |
| R5 | 1kΩ | 蜂鸣器三极管基极限流 |
| R6 | 10kΩ | nFAULT 上拉（开漏输出必须外部上拉） |
| Q1 | S8050 NPN | 蜂鸣器驱动 |
| SW1 | 6×6×5 轻触 | 抬杆 |
| SW2 | 6×6×5 轻触 | 落杆 |
| SW3 | SS12D00 拨动 | 总电源 |

> v1 的 **R1(0.33Ω) / R2(10k) / C6(100nF) 已删除** —— 见 §5.1。

### 2.3 端子

| 位号 | 脚数 | 引到哪 |
|---|---|---|
| J1 | 2P | N20 电机 |
| J2 | 3P | 双限位微动开关（共 GND） |
| J3 | 6P | INMP441 麦克风（引到闸体） |
| J4 | 2P | 103450 锂电 |
| J5 | 2P | 有源蜂鸣器 |

---

## 3. 网表

### 3.1 电源

| # | 网络名 | 连接点 |
|---|---|---|
| N1 | `BAT+` | J4.1, U3.B+ |
| N2 | `BAT-` | J4.2, U3.B- |
| N3 | `VBAT_PROT` | U3.OUT+, SW3.1 |
| N4 | `VBAT_SW` | SW3.2, U4.IN+, R3.1 |
| N5 | `+5V` | U4.OUT+, C1.1, C2.1, D1.阳极, U2.VM |
| N6 | `5V_MCU` | D1.阴极, C3.1, C4.1, U1.5V |
| N7 | `3V3` | U1.3V3, U5.VDD, J5.1, R6.1 |
| N8 | `GND` | U3.OUT-, U4.IN-, U4.OUT-, **U2.GND**, U1.GND, U5.GND, U5.L/R, C1.2, C2.2, C3.2, C4.2, C5.2, R4.2, SW1.2, SW2.2, J2.1, Q1.E |

> ⚠️ **N2 与 N8 是两个独立网络**。带保护板的 TP4056，`B-` 与 `OUT-` 之间隔着 FS8205 的 MOS 管，系统地必须取自 `OUT-`；接到 `B-` 会**绕过过放保护**。这是安全问题，不是风格问题。

### 3.2 电机

| # | 网络名 | 连接点 |
|---|---|---|
| N9 | `M_AIN1` | U1.GPIO9, U2.AIN1 |
| N10 | `M_AIN2` | U1.GPIO10, U2.AIN2 |
| N11 | `M_SLP` | U1.GPIO11, U2.nSLEEP |
| N12 | `M_FAULT` | U1.GPIO38, U2.nFAULT, R6.2 |
| N13 | `MOTOR_A` | U2.AOUT1, J1.1 |
| N14 | `MOTOR_B` | U2.AOUT2, J1.2 |

> B 通道不用：`BIN1`/`BIN2` **可以直接悬空**（手册 p3：所有逻辑输入均有 internal pulldown），`BOUT1`/`BOUT2` 悬空。
> `AISEN`/`BISEN` 按模块出厂状态（多数模块已在板上接地）。
> `nSLEEP` 有内部下拉 → **上电默认休眠**，电机不会在主控初始化前乱动，这是好事。

### 3.3 传感与人机

| # | 网络名 | 连接点 |
|---|---|---|
| N15 | `VBAT_DIV` | R3.2, R4.1, C5.1, **U1.GPIO1** |
| N16 | `LIMIT_UP` | U1.GPIO12, J2.2 |
| N17 | `LIMIT_DN` | U1.GPIO13, J2.3 |
| N18 | `BTN_UP` | U1.GPIO17, SW1.1 |
| N19 | `BTN_DN` | U1.GPIO18, SW2.1 |
| N20 | `MIC_SCK` | U1.GPIO4, J3.3 |
| N21 | `MIC_WS` | U1.GPIO5, J3.4 |
| N22 | `MIC_SD` | U1.GPIO6, J3.5 |
| N23 | `BUZ_CTL` | U1.GPIO2, R5.1 |
| N24 | `BUZ_BASE` | R5.2, Q1.B |
| N25 | `BUZ_DRV` | Q1.C, J5.2 |

J3 定义：`1=3V3, 2=GND, 3=SCK, 4=WS, 5=SD, 6=GND(L/R)`

**共 25 个网络。GPIO7/8/14/15/16/21/47 空闲预留。**

---

## 4. 防砸方案（行程超时反转）

无电流采样，靠时间判定：

- 记录一次完整行程的耗时 `T`（首次 HOMING 时标定）
- 落杆启动后若 **1.15 × T** 仍未触到落位限位 → 判定受阻 → 立即反向抬杆 + 蜂鸣报警
- 若 **1.5 × T** 仍未到位 → 进 FAULT，电机断电

**真实响应延迟**（必须知道）：

| 遮挡时机 | 检测延迟 |
|---|---|
| 行程刚开始就挡住 | ≈ 1.6s |
| 行程中段挡住 | ≈ 0.9s |
| 接近落位才挡住 | ≈ 0.2s |

这期间闸杆压在障碍物上的力约 **1.3N（≈130 克）**——N20 在 5V 下堵转扭矩约 2.0 kgf·cm，除以 150mm 力臂得出。对 6-10 岁儿童不构成伤害风险，但**延迟是秒级不是毫秒级**。

`nFAULT`（N12）是额外的硬件保护通道：过温、过流（2~3.3A）、欠压时芯片自己关断并拉低此脚。注意这个阈值远高于 N20 的实际电流，**它救不了防砸**，只在真正的电气故障时起作用。

---

## 5. 从数据手册核实出的结论

### 5.1 【已修正】v1 把采样电阻放错了位置

DRV8833 手册 p3-4 原文：

| 引脚 | 手册描述 |
|---|---|
| `GND` | "Device ground" |
| `AISEN` | "**Bridge A ground / ISENSE**" · "Connect to current sense resistor for bridge A, or GND if current control not needed" |

**H 桥的电机电流从 `AISEN` 回地，不经过 `GND` 脚。** v1 把 0.33Ω 串在 `U2.GND` 与系统地之间，那里只有芯片静态电流（几 mA），ADC 读不到有效信号，**防砸检测会完全失效**。已删除该电路。

（如果日后换成引出 `AISEN` 的模块或裸芯片：手册 §8.2.2.2 `R_ISENSE = 0.2V / I_CHOP`，V_TRIP 典型 200mV，0.33Ω → 606mA。届时可同时获得硬件斩波限流和快速堵转检测。）

### 5.2 逻辑输入内部下拉

手册 p3：`AIN1/AIN2/BIN1/BIN2/nSLEEP` 均标注 "Internal pulldown"。
→ B 通道输入可安全悬空（v1 写"悬空会乱翻转"是错的）；`nSLEEP` 上电默认低 = 休眠，符合安全预期。

### 5.3 TP4056 充电电流

模块上的 `Rprog` 决定充电电流，标准公式 `I_BAT = 1200 / R_PROG`（Ω→mA）：1.2kΩ→1A，**2kΩ→600mA**，2.4kΩ→500mA。

> ⚠️ 本条**未能从你给的 PDF 核实**——那份 TP4056 手册是扫描件，无法提取文字（正文页提取为空）。上述公式来自 TP4056 通用规格，请以模块实物或卖家标注为准。
> 多数成品模块出厂装 1.2kΩ（1A）。1800mAh 电池按 1A 充是 0.55C，偏大且小空间内发热明显；**建议把模块上那颗 SMD 电阻换成 2kΩ（600mA，0.33C）**，满充约 3.5 小时。

### 5.4 SX1308 与 MT3608 等价

两份手册对比（均为原文）：

| | SX1308 | MT3608 |
|---|---|---|
| 输入 | 2V to 24V | 2V to 24V |
| 开关频率 | 1.2MHz Fixed | 1.2MHz Fixed |
| 内部开关限流 | Internal 4A Switch Current Limit | Internal 4A Switch Current Limit |
| 最大输出 | Up to 28V | Up to 28V |
| 封装 | 6-pin SOT23 | 6-pin SOT23 |
| 标称效率 | up to 96% | up to 93% |

引脚定义相同（SW / FB / EN / VIN / GND）。**成品模块都是 `IN+ IN- OUT+ OUT-` 四焊盘，任选其一即可。** 输出电位器调到 **5.1~5.2V**（补偿 D1 的 0.3V 压降）。

### 5.5 DRV8833 手册要求的外围（模块已自带，确认即可）

| 引脚 | 手册要求 |
|---|---|
| VM | "A 10-μF (minimum) ceramic bypass capacitor to GND is recommended" |
| VINT | "Bypass to GND with 2.2-μF, 6.3-V capacitor" |
| VCP | "Connect a 0.01-μF, 16-V (minimum) X7R ceramic capacitor to VM" |

---

## 6. 布线约束

| 约束 | 原因 |
|---|---|
| **C1(470µF) + C2(100nF) 紧贴 U2 的 VM/GND 引脚**，走线短而粗 | 最容易翻车的地方：N20 启动电流是运行的 3~5 倍，升压输出瞬间塌陷会让主控 brownout 复位，表现为"一按遥控器整机重启" |
| **D1 必须在 C3 之前** | 电机拉低 +5V 时 D1 反向截止，C3 独立供住主控 |
| 电机走线（N13/N14）线宽 ≥ 1mm | 峰值约 800mA |
| 星形地汇于 U3.OUT- 附近一点 | 避免电机地电流流经主控地 |
| **J3（麦克风）走线远离电机走线和 U4 升压模块** | 升压 1.2MHz 开关噪声 |
| SW1/SW2 对齐上盖板 X=-50 / -30 的按键孔 | 结构已定死 |
| U3 的 Type-C 口朝 -X 端面 | 结构已定死 |

---

## 7. 与结构的接口（CAD 里已定死）

| 项 | 值 |
|---|---|
| 板子最大外形 | 146 × 51 mm，元件高 ≤ 13mm |
| 按键孔位 | X = -50 / -30，Y = 0，φ6.5 |
| Type-C 开孔 | 前端面 X = -75，10×5mm，离底 8mm |
| 总电源开关孔 | 前端面，8×4mm，Y = +19 |
| 走线孔（去闸体） | X = +25，Y = -16.5，φ8 —— **J1/J2/J3 靠近这里** |
| 电池仓 | X = -71~-21，Y = ±17（板子避让） |
| 主控位置 | 中心 (11, 10)，63 × 25.5 |

---

## 8. 尚未确认（按"最常见"假设，到货核对）

| 项 | 当前假设 | 到货要核对 |
|---|---|---|
| DRV8833 模块排针 | 1×10P，丝印 `VM GND AIN1 AIN2 BIN1 BIN2 AO1 AO2 BO1 BO2`，`nSLEEP` 可能标 `SLP`/`STBY`，`nFAULT` 可能标 `FLT` | 排针数量、间距、丝印名 |
| TP4056 模块 | 4 焊盘 `B+ B- OUT+ OUT-` | 丝印是否把 OUT 简写成 `+ -` |
| 升压模块 | 4 焊盘 `IN+ IN- OUT+ OUT-` | 一般无差异 |
| DevKitC-1 | 2×22P，行距 22.86mm | 板子实际排针数 |

**这些不确认就打样是在赌**。但按最常见封装先画，到货后改排母间距参数比重画快得多。
