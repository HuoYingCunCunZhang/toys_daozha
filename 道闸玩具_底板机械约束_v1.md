# 承载底板 · 机械约束 v2

> 给"转 PCB / 布局"这一步用。**元件尺寸一律以 [元器件尺寸基准 v1](道闸玩具_元器件尺寸基准_v1.md) 为准**，本文只讲结构对 PCB 的约束。

**坐标系**：原点 = 底座几何中心；+X = 闸杆伸出侧，−X = 前端面（Type-C / 总开关 / 按键侧）；Y = 车行方向；Z 向上，Z=0 是底座底面外表面。

---

## 一、总体尺寸（已定案）

> 🔴 **2026-08-14 放大：底座 183 × 73 × 25、承载底板 120 × 65。** 原因见 [§七之二](#七之二pcb-布局2026-08-14布局已定稿待布线)。下表已按新值更新。

| 项 | 值 |
|---|---|
| 底座外形 | **183 × 73 × 25** |
| 托盘内腔 | x ±89.5，y ±34.5，地板面 z=2 |
| 上盖 | 厚 3，背面（内腔天花板）z=**22** |
| **承载底板** | **120 × 65 × 1.6** |
| 板底面 z | **4.0**（坐在 4 根 φ4.4 支撑柱上）|
| **板面 z** | **5.6** |

换算关系（改板子尺寸时底座按此跟随）：

```
BASE_L = 板长 + 63     BASE_W = 板宽 + 8
```

---

## 二、板子外形与安装孔

```
        y
   +32.5┌───────────────────────────────────┐
        │╲                                  │
   +27  │ ╲   ○安装孔                 ○     │
        │                                   │
        │                                   │
   -27  │ ╱   ○                       ○     │
        │╱                                  │
   -32.5└───────────────────────────────────┘
        -88                              +32   x
        └ 5×5 切角 ×2（让开底座角螺柱）
```

| 项 | 值 |
|---|---|
| 外形 | x **−88 … +32**（长 **120**），y **−32.5 … +32.5**（宽 **65**）|
| **−X 两角倒角** | 🔴 **45° × 7mm**（2026-08-15 由 5 加大），让开底座角螺柱（螺柱在 (±87, ±32)，φ6）|

> 🔴 **倒角从 5 加到 7 是因为 5 装不进去。** 2026-08-15 第一次把 EDA 里的实际板框读回来和 CAD 对照才发现两件事：
>
> 1. **CAD 一直把这两个角建成 5×5 的方形缺口，而实际板框是 45° 倒角。** 方缺口挖掉的料更多，所以自检 `t=21` 一直显示"没问题"——**CAD 在偏乐观的方向上错了**，这类错误比报错更危险。
> 2. 按真实倒角算：角螺柱中心 (−87, 32) 到倒角斜边只有 **2.47mm**，而螺柱半径 3.0 → **板子会啃进螺柱 0.53mm**。
>
> 倒角要 ≥6.2 才让得开，取 **7** → 净距 **0.89mm**。已确认新增挖除区内没有任何铜，EDA 板框已改、覆铜已重灌、DRC 零错误。
>
> **教训**：文档里的尺寸必须回源头（EDA）验一次。这条和 DevKit 封装行距那次是同一个根因。
| 安装孔 ×4 | φ2.2 **非金属化**，位于 **(−82, ±27)** 和 **(+26, ±27)**；φ4.5 以内不要走线 |
| 安装螺丝 | M2×5 十字盘头自攻 |

**板底只有 2.0mm**：直插件焊脚必须剪到 **≤1.5mm**。0603 / SOT-23 这类贴片件**可以放底面**，建议把 R3/R4/R5、Q1、C2/C4/C5 全挪到底面，给顶面腾地方。

---

## 三、高度预算

| 区域 | 范围 | 天花板 z | **板面以上可用** |
|---|---|---|---|
| **中央区** | \|x\| < 82.75 且 \|y\| < 27.75 | 22.0 | **16.4mm** |
| 止口环下方 | 板子四周约 6.5mm 宽的边带 | 20.0 | 14.4mm |

> 中央区边界 = `(BASE_L−2·WALL−2·GAP)/2 − LIP_W`。2026-08-14 CAD 解冻时更正：
> 这两个数原来写的是 72.75 / 22.75，那是**底座 163×63 时代**的值，底座放大到
> 183×73 后没跟着改。现值由 CAD 参数 `LIP_IN_X` / `LIP_IN_Y` 直接给出。

> 🔴 **中央区里另有两块地被按键帽占了**：上盖背面在 (0,+20) 和 (+20,+20) 各有一个
> **φ14 × 高 2.5 的导向凸台**（底面 z=19.5），给按键帽当导向。那两处板面以上只剩
> **13.9mm**。轻触开关本身顶面才 17.6，让开 1.9mm ✓，但别在按键正下方放高件。

各器件叠高校核：

| 器件 | 叠高 | 余量 |
|---|---|---|
| U1 DevKit + **低背 5.0 排母** | 12.17 | ✅ 4.2 |
| U2 DRV8833 + 低背 5.0 排母（模块高 5.0）| 12.5 | ✅ 3.9 |
| U3 TP4056 **贴板直焊** | 4.8 | ✅ 11.6 |
| U4 MT3608 **贴板直焊**（**37×17**×7）| 7.0 | ✅ 9.4 |
| C1 φ6.3×11 / C3 φ5×11 | 11 | ✅ 5.4 |
| SW1/SW2 轻触 6×6×12 | 12 | ✅ 顶杆本来就要伸进上盖孔 |
| J1/J2/J4 KF128-3.81 | ~9.5 | ✅ |

> **排母只买低背 5.0mm。** DevKit 叠高是四层：排母 5.0 + DevKit 排针塑料隔片 **2.5** + DevKit 板 1.57 + 模组 3.1。用常见的 8.5mm 排母就是 15.67mm，余量几乎为零。

---

## 四、位置约束（**结构让路给 PCB**，不是反过来）

> 🔴 **2026-08-14 用户更正了本节的原则。** 原标题写的是"结构已开孔，PCB 迁就"——**这是反的**，与[尺寸基准](道闸玩具_元器件尺寸基准_v1.md)里定的"**先以板子尺寸为主，结构件随板子调整**"自相矛盾。
>
> **正确顺序：先把电路板设计好，再按 PCB 的实际结果去改结构尺寸。**
> 安装孔位置、按键位置、卧式开关位置**都可以为布线让路**，最后以 PCB 定稿的实际坐标反推结构。
>
> 仅有的软性要求（不是硬约束）：
> - 两个按键**尽量在同一条水平线上**（同一 Y），且不要太偏
> - 卧式开关（SW3）位置可适当调整
> - **整体上不要大动**，保持外观比例合理
>
> 下表是**当前 PCB 定稿后的实际值**，供结构跟随，不是给 PCB 的约束。

**元件的实际坐标只有一份，在 [§七之二 元件坐标](#元件坐标v3-定稿按可布线性重排)。** 本节不再重复列表（曾经重复过，结果两处对不上）。

结构需要跟随的接口，只有这几条：

| 结构特征 | 依据 PCB 的哪个元件 | 当前值 |
|---|---|---|
| 上盖 φ9 按键孔 ×2 | SW2（落杆）/ SW1（起杆）| **(0, +20)** 和 **(+20, +20)**，同一 Y，间距 20 |
| 前端面 Type-C 开口 | U3 | 座中心 y ≈ **−6**、z ≈ 8；本体前沿 x = −88.23。**开口 12.5×8.0**（按插头塑料头 11×6.5 开，见 §287）|
| 前端面拨柄开口 | SW3 | 拨柄尖 x = **−93.51**、中心 y ≈ **+17**、z ≈ 8。露出外壁 **2.01mm** ✅ |
| 上盖充电灯观察孔 | U3 的两颗指示灯 | CAD 已开在 **(−80, 0) 的 10×6 长圆窗** ✅ 位置由用户按实物给定（原 (−84,−6) 是"指示灯紧邻 Type-C 座"的猜测值，已作废）|
| 上盖走线孔 | J1 电机 / J2 限位 / J3 麦克风 | 三者已按布线需要分散，**不再统一靠 +X 板边**；走线孔位置按闸体位置定即可，内部飞线长一点无妨。CAD 实建 **(55, −16.5)**，在电池上方 |
| **闸体位置** | SW1 起杆按键 | 🔴 **闸体从 x=25 挪到 x=55**（占 x 32…78）。原来占 2…48，把 x=+20 那颗按键整个盖住了 |
| 底板支撑柱 ×4 | 安装孔 | **(−82, ±27) (+26, ±27)** |

> **按键 y 的约束**：φ9 的孔要整个待在中央区，顶杆才不会撞止口环；同时要让开 27.94 宽的 DevKit。当前取 y=+20，DevKit 顶边在 +5.97，让开 ✓

---

## 五、~~建议分区~~（已作废，被 §七之二 的实际布局取代）

> 本节原按 100×55 的板子做的分区建议，板子已放大到 120×65 且布局已按可布线性重排完成，**这张分区表全部作废**。以 [§七之二 元件坐标](#元件坐标v3-定稿按可布线性重排) 为准。
>
> 唯一仍成立的一条经验：**贴片矮件不必占板面**——DevKit / DRV8833 插排母后架空 7.5mm，0603 / SOT-23 / SMA 可以直接放在模块肚子底下（**11mm 高的电解电容 C1/C3 不行**）。当前布局已用上这一点。

---

## 六、托盘那边的让位（别撞）

> 下表是 **CAD v3.0 实建值**（2026-08-14 解冻后回写，与 `cad/daozha.scad` 一致）。

| 特征 | 位置 | 高度 z |
|---|---|---|
| 角螺柱 ×4 | (±87, ±32)，φ6 | 1 → 19.8 |
| 底板支撑柱 ×4 | **(−82, ±27) (+26, ±27)**，φ4.4 | 0 → 4 |
| 电池仓 −X 挡边 | x 33…36 | 1 → 12 |
| 电池仓 ±Y 挡边 | **y ±18…±21**（贴着电芯放 1mm，不是贴内壁）| 1 → 12 |
| 电池 103450（成品）| x 36…87，y ±17 | 2 → 12 |
| 闸体固定螺丝头 | **(40,±15) (70,±15)** | 沉平在上盖背面 z=22，**不伸进腔内** |
| 走线孔（上盖）| **(55, −16.5)**，φ8 | 贯穿上盖 |
| 按键帽导向凸台 ×2（上盖背面）| (0,+20) (+20,+20)，φ14 | 19.5 → 22 |
| 充电灯观察窗（上盖）| (−84, −6)，10×6 长圆 | 贯穿上盖 |

> 🔴 **闸体从 x=25 挪到 x=55**（占 x 32…78）。原因：PCB 定稿的起杆按键在 x=+20，
> 而闸体原来占 x=2…48，**整颗按键被闸体盖住，手指够不到**。按"结构让路给 PCB"
> 只能挪闸体。连带上盖走线孔从 (25,−16.5) 挪到 (55,−16.5) —— 落在电池上方，
> 闸体下来的飞线贴电池顶面（有 10mm 空隙）往 −X 走到板上，与电池自己的引线同路。
>
> ⚠ 原表里 (±31…±34) 的电池仓 ±Y 挡边是错的：那个位置贴着内壁，电芯会在 Y 向
> 晃 14mm。实建按"比电芯每边放 1mm"取 ±18…±21。

---

## 七、EDA 里必须先改掉的 3 处封装 + 1 条网络

**转 PCB 之前先做完这些，否则焊盘全错。**

| # | 位号 | 改什么 | 状态（2026-08-13 晚）|
|---|---|---|---|
| 1 | **SW3** | 器件换 **SK12D07VG7**，信号脚间距 **2.54 → 2.0**，另有两端固定脚，总跨 8.3 | ✅ **已做**。系统库器件 `SK-12D07VG7`，焊盘实测 2.0 / 8.3 全对 |
| 2 | **U2** | 封装 1×10 单排 → **2 排 × 8 脚**，排间距 **15**（建议画椭圆焊盘吸收 15/15.24 差异）| ✅ **已做**，自建封装，排距取 **15.24**（见下）|
| 3 | **U4** | 封装 1×4 排母 → **四角 4 个焊盘**，通孔 φ**1.651**，焊盘 4×4mm；模块外形 **37×17**（原写 36，已按实物图更正）| ✅ **已做**，孔距实测 **30 × 7**，自建封装 |
| 4 | **N12 / R6** | **删除**。`M_FAULT` 网络和 10kΩ 上拉都不要了——实物 DRV8833 模块没引出 `nFAULT` 脚。**GPIO38 释放为空闲** | ✅ **已删** |

> J3（麦克风）**不用改**，2.0mm 本来就对——麦克风在闸体上靠飞线接过来，间距不需要与模块一致。

### U2 排距为什么取 15.24 而不是 15

原文建议"用椭圆焊盘吸收 15/15.24 差异"，但这条**推理链其实不成立**：模块靠两条 1×8 排母插着，排母条本身是刚性的、脚距固定 2.54，**我们板上焊盘的形状根本影响不到模块能不能插进去**——那个 0.24mm 是由排母插孔自身的顺应量吃掉的，不是由焊盘吃掉的。

真正该定对的是**孔位**。8 个脚必然落在 0.1″ 栅格上，所以 **6 × 2.54 = 15.24** 才是物理上说得通的数，"15" 应是读数取整。已按 15.24 建封装；φ1.0 孔对 0.5mm 排母针本身有 ±0.25mm 余隙，两种说法都能插。
椭圆焊盘照做了（2.2 × 1.8，长轴沿排距方向），当作铜箔余量，不当作公差补偿。

**打样前拿卡尺复核一次两排针中心距**；若确为 15.0，把封装两列 x 从 ∓7.62 改成 ∓7.5 即可。

### ✅ U4 孔距已实测：30 × 7

| | 值 | 每边余量 |
|---|---|---|
| 长边（36）方向两孔中心距 | **30** | 3 |
| 短边（17）方向两孔中心距 | **7** | 5 |

焊盘坐标 **(∓15, ±3.5)**，孔 φ1.651（=65mil），焊盘 4×4。封装：个人库 `MODULE-TH_MT3608-37x17-4PAD`。
**模块外形是 37 × 17，不是 36**（实物尺寸图，见尺寸基准）。
原理图 U4 符号是 4 脚、**没改**，只换了封装，四条连接改前改后完全一致。
**正极排布已按实物确认**："+"走一条长边、"−"走另一条。

> ⚠ **PCB 布局时要注意一件事**：焊盘 4×4 而短边孔距只有 7 → **两焊盘边缘只剩 3mm 净距**，且焊盘外缘到模块短边只有 (17−7)/2−2 = **3mm**。布局时 U4 下方尽量不要走线，避免大焊盘的热量和铜箔挤占。
> ⚠ **转 PCB 后顺手核一下 U4 丝印外框是 37 还是 36**（工程库副本可能没跟着更新，焊盘无差异）。见 [尺寸基准 §遗留](道闸玩具_元器件尺寸基准_v1.md)。

---
## 七之二、PCB 布局（2026-08-14，已定稿）

### 🔴 板子和底座放大了：120 × 65（底座 183 × 73 × 25）

原 100×55 装不下。根因是 **U4 实测 37mm 长**，而当初定 100×55 时 U4 假设的还是 SX1308（23×16.3）——这 14mm 是一整块刚性尺寸，挪不动也拆不开。

100×55 上算过的死结：U4 需要 37mm 连续跨度，而板上唯一够长的位置被 SW2（按键孔钉死）和右安装孔夹成 **34.8mm**，差 3.2mm。硬塞需要把安装孔挪到离板边只剩 1.4mm 板材，且全板只剩 0.3mm 级间隙。

用户拍板放大。**120 × 65**：占用率 71% → **51%**，最小间距回到 0.87mm 以上。

| | 原 | 新 |
|---|---|---|
| 承载底板 | 100 × 55 | **120 × 65** |
| 底座（按 `BASE_L=板长+63`、`BASE_W=板宽+8`）| 163 × 63 × 25 | **183 × 73 × 25** |
| 板子占位（原点=底座几何中心）| x −78…+22, y ±27.5 | **x −88…+32, y ±32.5** |
| 安装孔 ×4 | (−70,±23) (+16,±23) | **(−82,±27) (+26,±27)** |

−X 两角仍是 **5×5 切角**。安装孔 φ2.2 **非金属化**。

### ⚠ 按键位置最终定为 (0, +20) 和 (+20, +20)

历史上三份文档给过三组值，全部作废：本文档 v2 写 (−45,+12)(−25,+12)，[网表基准](道闸玩具_底板网表基准_v2.md) 和[需求书](道闸玩具_需求规格说明书_v2.md)写 X=−50/−30，中途还统一过一次 (−50,+12)(−30,+12)。

**布局 v3 按可布线性重排后，最终值是 SW2 (0, +20)、SW1 (+20, +20)** —— 同一 Y、间距 20mm，紧邻 U1 的 `BTN_DN`/`BTN_UP` 引脚。上盖 **φ9** 按键孔按此开（不是旧文档写的 φ6.5）。
### 元件坐标（v3 定稿，按可布线性重排）

**排布依据：外围件的左右次序 = U1 那排引脚的次序。** U1 有 14 个信号挤在同一排，从左到右是：

```
GND · 5V_MCU · LIMIT_DN · LIMIT_UP · M_SLP · M_AIN2 · M_AIN1 · … · BTN_DN · BTN_UP · … · MIC_SD · MIC_WS · MIC_SCK · 3V3
```

所以上方外围件从左到右依次是 **J2(限位) → U2(电机驱动) → SW2/SW1(按键) → J3(麦克风)**，飞线基本不交叉。
另一排只有 `BUZ_CTL` 和 `VBAT_DIV` 两个信号（在 U1 下方），对应的 Q1/R5/LS1 和 R3/R4/C5 就放在 U1 底下那条带。

| 位号 | x | y | rot | 说明 |
|---|---|---|---|---|
| **U3** TP4056 | −74.8 | −6 | 0 | Type-C 朝 −X，本体前沿悬出板边 0.23 |
| **SW3** 拨动 | **−84.1** | +17 | 270 | 拨柄尖 x=**−93.51**，露出外壁 2.01mm。🔴 原来在 −82.6，拨柄只露 0.51mm，**不满足尺寸基准 §366 定的 1mm 判据**——那张 H 选型表是按老底座（外壁面 −75）算的，底座放大到 183 后没回头重算。物料已买齐换不了 H，改成往 −X 挪 1.5mm 补回来（焊盘到板边仍有 3.9mm）|
| **U4** MT3608 | −60 | +23.5 | 0 | 靠左，紧邻 `VBAT_SW` 的来源 SW3 |
| **J2** 限位 | −58 | +10 | 0 | |
| **C3** 220µF | −46 | +10 | 0 | 11mm 高，不能放模块底下 |
| **U2** DRV8833 | −31 | +20 | 0 | 正对 U1 的 `M_AIN1/2`、`M_SLP` |
| **C2** 100nF | −26 | +27.3 | 0 | **在 U2 肚子底下**，紧贴 VM/GND |
| **C1** 470µF | −17.5 | +28 | 0 | 距 U2 的 VM 焊盘约 6mm |
| **J1** 电机 | −13 | +18 | 0 | 紧邻 U2 的 AO1/AO2 |
| **U1** DevKit | −16 | −8 | 270 | 占 x[−47.81,+15.81] y[−21.97,+5.97] |
| **SW2** 落杆 | 0 | +20 | 0 | 与 SW1 **同一 Y**，间距 20mm |
| **SW1** 起杆 | +20 | +20 | 0 | 同上 |
| **J3** 麦克风 | +22 | +9 | 0 | 远离电机走线与 U4 |
| **J4** 电池 | +24 | −10 | 0 | 靠 +X 板边 |
| D1 | −40 | 0 | 0 | **U1 肚子底下** |
| C4 | −34 | 0 | 0 | U1 肚子底下 |
| R5 | −5 | −16 | **180** | U1 肚子底下；转 180° 是为了让 BUZ_CTL/BUZ_BASE 不交叉 |
| C5 | 0 | −16 | 0 | U1 肚子底下 |
| R3 | +5 | −16 | 0 | U1 肚子底下，紧邻 U1 的 ADC 脚 |
| R4 | +9 | −16 | 0 | U1 肚子底下 |
| Q1 | −4 | −24.6 | 0 | |
| LS1 蜂鸣器 | **+7** | −27.2 | 0 | |

> **利用了模块架空的空间**：DevKit 插 5.0 排母后板底离板 **7.5mm**，0603 / SOT-23 / SMA 这类矮件可以直接放在它（以及 U2）肚子底下。**C1 / C3 是 11mm 高的电解电容，不行。**

### 布局质量

| 指标 | 结果 |
|---|---|
| **总 HPWL**（各网络包围盒半周长之和）| **1711 → 1010 mm，改善 41%** |
| `MOTOR_A` / `MOTOR_B` | **99/105 → 12/13 mm** ✅ 文档要求"短而粗"的两条 |
| `VBAT_DIV`（ADC 高阻节点）| 126 → 14 mm ✅ |
| `BUZ_CTL` | 71 → 11 mm ✅ |
| 元件重叠 | **0** |
| 最小间距 | **0.93mm** |
| 焊盘全部在板内 | ✅ 最小边距 1.47mm（U3.5）|
| 安装孔净空冲突 | **0** |

### 🔴 安装孔"净空 0 冲突"不等于螺丝拧得下去（2026-08-15 补）

上面那行只查了"孔里有没有东西"，**没查螺丝头底下压着什么**。复核发现：

- 四个孔位 **(−82,±27)、(26,±27)** 与 CAD `PCB_MNT` **零偏差**；孔径 **φ2.200 非金属化孔**（焊盘=孔径无环），M2 φ2.0 单边 0.1mm ✅
- 🔴 但 **(−82,27)** 那个孔，`VBAT_SW` 走线铜边离孔心只有 **1.50mm**；而覆铜区是矩形 `["R",−3340,1165,4515,2315]` mil（x −84.84…29.85、y −29.21…29.59），**四个孔全在里面**，板上没有任何禁铺铜区。
  M2 盘头 φ3.5~4.0（半径 1.75~2.0）→ **螺丝头同时压住 `VBAT_SW` 和 GND 覆铜，中间只隔一层阻焊**。拧紧压破阻焊 = **电池正极对地短路**，而 `VBAT_SW` 是拨动开关之后的电池正极，全程无限流。
- **已修**：`VBAT_SW` 的拐角从 x=−80 挪到 **−78.5**（并补一段 (−80,17)→(−78.5,17) 保住 T 型接点）→ 净距 **1.50 → 3.00mm** ✅。其余三孔 4.69 / 9.29 / 13.64mm 本来就够。
- 判据：**孔心到任何非 GND 铜箔 ≥ 2.5mm**（螺丝头半径 2.0 + 0.5 位置公差）。

> **教训**：这类"孔位完全对齐但装不了"的问题，孔位表怎么查都查不出来 —— 要查的是**螺丝头投影范围内有什么**。

> ⚠ **唯一变长的网络是 `VBAT_SW`（81 → 131mm）**，这是有意的取舍：R3 必须靠近 U1 的 ADC 脚以缩短 `VBAT_DIV`——那是 100k/100k 分压的高阻节点（源阻抗 50k），怕噪声；而 `VBAT_SW` 是低阻电池轨，走长一点无害。

### 🔴 U1 DevKit 封装的行距是错的，已修正

系统库 `CONN-TH_ESP32-S3-DEVKITC-1U-N8` 两排焊盘间距 **22.86mm**，而卖家尺寸图明确标 **25.400mm**（板宽是另一个标注 27.940）。
几何上也是尺寸图对：27.94 板宽配 25.4 行距 → 每排距板边 1.27mm，排针本体正好与板边齐平。**照库封装打样，DevKit 插不进去。**

已复制到个人库改为 `CONN-TH_ESP32-S3-DEVKITC-1-N16R8_S25.4`，44 个焊盘 X 从 ±11.43 → **±12.7**。新建器件 `ESP32-S3-DevKitC-1-N16R8-行距25.4` 沿用原符号，故原理图 20 根导线一根未动，网表逐条比对完全一致。

> ⚠ 该封装的**丝印**是非线图元画的，API 读不到也改不了，外框仍按旧板宽画，比实际窄。不影响电气与装配，只是目视参考偏小。

### ⚠ U3 的「悬出板边 2mm」做不到，实际 0.23mm

`PWRM-TH_TP4056` 里 IN+/IN−（脚 5/6）距原点 11.73，而本体前沿距原点 13.43 —— 本体只比最外侧焊盘多伸 1.7mm。要悬出 2mm 这两个焊盘就掉到板外了。
现取本体前沿与板边基本齐平（U3 在 x=−74.8，本体前沿 −88.23，悬出板边 0.23）。

> 🔴 **原来这里写的"USB-C 插头长约 6.5mm，穿 2mm 壁 + 1.7mm 间隙仍插得到底"是错的**（2026-08-15 复核推翻）。
> 那句话只比了"插头长度 > 死区"，**漏了插头的塑料头也得跟着钻进去** —— Type-C 完全插入时塑料头前端面基本贴住座口。
> 从外壁面 −91.5 到座口 −88.23 有 **3.27mm 死区**，塑料头（实测 **11 × 6.5**）过不去 10×5 的开口就会顶在外壁上，
> 金属壳只能进 6.5 − 3.27 ≈ **3.2mm，约一半，接触不上**。
> **修法：开口按塑料头开，不是按金属壳开** —— `USB_W/H` 10×5 → **12.5×8.0**（`USB_Z` 8 → 8.5）。遥控器同理，死区 3.5mm，`RC_USB` 10×4.5 → 12.5×8.0。
> 自检 `t=80..83` 钉死这条，阳性对照是"塑料头放大到 14×9 必须被挡住"。

### ⚠ 两个通用教训

1. **`getPrimitivesBBox()` 把位号文字也算进去**，不能当实体尺寸用（U2 丝印 18.1 宽，包围盒报 21.08）。算净距必须用实体尺寸。
2. **元件间距不能只比单轴**。两个盒子在 X、Y 都不重叠时是对角相邻，真实间距是对角线长度，只看 X 会误报成"几乎贴住"。

---

## 七之三、PCB 布线（2026-08-14，**已完成，DRC 零错误**）

### 结果

| 项 | 值 |
|---|---|
| 走线 | **105 段**，双面 |
| **过孔** | **0**（全部靠 THT 焊盘换层）|
| 覆铜 | **顶层 + 底层各一块 GND** |
| **DRC** | **✅ 零错误**（`pcb_Drc.check(true,false,true)` 返回空数组）|
| 独立连通性复核 | ✅ 23 条信号网络全部连通 |

线宽按电流分档：**电机 1.0mm**、**电池回路 1.0mm**、`+5V` 0.6mm、其它电源 0.4~0.5mm、信号 0.3mm。

#### 🔴 复核发现：电池回路原线宽不够，已加粗

按 IPC-2221（1oz 外层、10°C 温升）核算，最坏工况是**电机峰值 800mA + 电池 3.0V 低电量**：
升压输入端电流 = 5.1 × 0.8 ÷ 0.93 ÷ 3.0 ≈ **1.46A**。

| 网络 | 原线宽 | 承载能力 | 判定 | 现改为 |
|---|---|---|---|---|
| `VBAT_PROT` / `VBAT_SW` | 0.5mm | 1.45A | ❌ **不足** | **1.0mm**（2.39A）|
| `BAT+` / `BAT-` | 0.6mm | 1.65A | ⚠ 仅余 13% | **1.0mm** |

> **注意**：电池侧电流**大于** +5V 侧（1.46A vs 0.8A），因为升压是降流升压。容易只盯着输出侧而漏掉输入侧。

### 关键网络的走线依据

| 网络 | 层 | 线宽 | 依据 |
|---|---|---|---|
| `MOTOR_A` / `MOTOR_B` | 顶 | **1.0** | 网表基准要求 ≥1mm；布局已把长度压到 12/13mm |
| `+5V` → U2 的 VM | 顶 | 0.6 | "最容易翻车的地方"：C1/C2 紧贴 VM，走线短而粗 |
| `BAT+` / `BAT-` | 底 | **1.0** | 电池主回路（审核后加粗，见下）|
| `GND` | **两面覆铜** | — | 22 个 GND 焊盘全靠它；星形地的实现方式 |

### 🔴 J2 的两个限位端子对调了（改了原理图）

`LIMIT_UP`/`LIMIT_DN` 原本必然交叉：J2 上的源顺序与 U1 上的目标顺序是**反的**。

限位开关是**靠飞线接到 J2**的，哪个端子接哪个信号完全自由。于是把原理图里 J2 的网络端口对调：

| | 改前 | **改后** |
|---|---|---|
| J2.2 | LIMIT_UP | **LIMIT_DN** |
| J2.3 | LIMIT_DN | **LIMIT_UP** |

改完源序与目标序一致，交叉消失，两条线同层平行走完，**省掉一个过孔**。

> ⚠ **接线时注意**：J2 的第 2 脚接**落杆**限位、第 3 脚接**起杆**限位。装配文档要按此写。

### 为什么最终 0 过孔

`pcb_PrimitiveVia.create()` 建出来的过孔，位置/网络/类型（通孔）/尺寸全部正确，加大到 0.9mm 重建后 **EDA 仍报"该走线与同网络其它对象断开"**，即不承认它导通。原因未查明。

绕开办法：**所有换层都发生在 THT 焊盘上**（THT 焊盘天然贯通两层，不需要过孔）。为此做了两处调整：

- `VBAT_PROT` 改走底层（两端 SW3.1 / U3.1 都是 THT），让出顶层给 `VBAT_SW` 全程走完
- J2 端子对调（见上），消掉 `LIMIT` 那对交叉

### 自动布线不可用

`pcb_Document.autoRouting()` 无论带不带参数、指定单条网络与否，**一律返回 24 条全部失败、`duration: 0`**，不产生任何铜。界面上点按钮则报「请先绘制板框」。

但**板框是好的**：`zoomToBoardOutline()` 返回 true、3D 预览能生成（3D 必须有板框）、6 段线端点严丝合缝闭合。判断是该功能自身受限（云端服务/授权），**不是板框问题**。改 API 建单个闭合图元也走不通——`Polyline`/`Region`/`Pour` 都要 `IPCB_Polygon` **对象**，而该对象无公开构造方法。

### 覆铜必须手工画 + 画完要重建

同样因为 `IPCB_Polygon` 无法构造，**覆铜只能在界面上画**（顶层、底层各一块，网络 GND，比板框内缩约 0.5mm）。

⚠ **画完必须重建**：`IPCB_PrimitivePour.rebuildCopperRegion()`，否则铜箔与走线间距为 0，等于把底层全部短接到 GND。

⚠ **底层覆铜够不着顶层贴片的 GND 焊盘**（C2.2 / C4.2 / C5.1 / Q1.2 / R4.1），所以**两面都要铺**。

### 布线方法：先算后画

写了两个校验脚本，**在下笔之前**算间距，通过了才创建：

| 脚本 | 作用 |
|---|---|
| `clearance.js` | 每段走线对**所有异网焊盘**和**同层异网走线**算实际净距，阈值 0.2mm |
| `connect.js` | 用**回读的实际铜箔**重建连通性，确认每条网络的焊盘真的在同一连通块 |

**它一共拦下 30+ 处问题**，其中三处是校验模型自身的缺陷，值得记住：

1. **U1 的 24 个未用引脚也要避让** —— 漏算后 `BUZ_BASE` 撞 U1.38、`VBAT_SW` 撞 U1.42。U1 两排焊盘是 2.54 满排，**穿越只能走 1.02mm 的缝**（0.3mm 线居中约剩 0.36mm）
2. **贴片焊盘只在顶层**，不该双面阻挡（修正后消除 8 处误报）
3. **安装孔是独立焊盘**，`getAllPins()` 取不到 —— 漏算后 `VBAT_SW` 蹭到 MH1，是 DRC 抓出来的


## 八、变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1 | 2026-08-12 | 首版，底座 150×55×22，板 87×47 |
| **v2** | **2026-08-13** | 底座放宽到 **163×63×25**、板 **100×55**；净空 13.4→**16.4**；升压改 MT3608 摊明面直焊；DRV8833 高 5 可插排母；按键孔 φ6.5→**φ9** 且位置改 (−45/−25, +12)；新增"TP4056 指示灯不可被 DevKit 遮挡"；J3 不用改封装（更正） |
| **v2.1** | **2026-08-14** | 板 **120×65** / 底座 **183×73×25** 定稿，PCB 布局布线完成（§七之二、§七之三）|
| **v2.2** | **2026-08-14** | **CAD 解冻到 v3.0 后回写实建值**：§三 中央区边界更正为 ±82.75/±27.75（原来还是 163×63 时代的 72.75/22.75）；§六 全表按实建更新，电池仓 ±Y 挡边由 ±31…±34 更正为 **±18…±21**（原值贴着内壁，电芯会晃 14mm）；新增按键帽导向凸台和充电灯观察窗；🔴 **闸体 x=25 → 55**（原位置整个盖住 x=+20 那颗按键）|
