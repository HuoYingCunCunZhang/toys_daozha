# 道闸玩具 · 电路与结构设计方案 v2.5

> ⚠️ **物理尺寸以 [元器件尺寸基准 v1](道闸玩具_元器件尺寸基准_v1.md) 为准。**
> 本文档中的元件尺寸只作方案说明，与基准表冲突时以基准表为准。
> ⚠️ **坐标约定**：本文档沿用旧约定（**Y=高度、Z=车行方向**），而 CAD 与基准表用 **Z=高度、Y=车行方向**。读本文时请把 Y↔Z 对调。

> 依据：`道闸玩具_需求规格说明书_v2.md`
> v2.0 → v2.1 变更：闸杆改从**按键相反侧**伸出、起杆角度 80°→**90°**、转轴高度 76→**42mm**、底座按键 3→**2** 个、Type-C 移到**前端面**、**取消爆闪灯/天窗**、**取消 VL53L0X**（改堵转检测防砸）

---

## 1. 系统总览

```
┌──────────────────────────┐            ┌──────────────────────────────────────────┐
│  遥控器 60×30×20          │            │  道闸主机                                  │
│  ESP32-C3 SuperMini      │  ESP-NOW   │  ┌── 闸体 46×46×62（黄，纯净顶面）──────┐  │
│  · 抬杆 / 落杆 / 复位 三键 │◄─────────►│  │ N20 6V 15rpm + 转毂 + 双限位微动      │  │
│  · 板载 WS2812 状态灯     │  2.4G 直连 │  │ 90° 机械止挡筋 · 配重仓               │  │
│  · 502030 锂电 250mAh     │  固定信道1 │  │ INMP441 麦克风（前面板格栅）           │  │
│  · TP4056 Type-C          │            │  └──────────────────────────────────────┘  │
│  · 深度睡眠 <10µA         │            │  ┌── 底座 150×55×20 ────────────────────┐  │
└──────────────────────────┘            │  │ ESP32-S3-DevKitC-1 N16R8（平放）      │  │
                                        │  │ 承载底板 · DRV8833 · 升压              │  │
     ◄── -X 前（长脚）                    │  │ TP4056 · 103450 锂电 1800mAh          │  │
     按键×2 · Type-C · 总开关             │  │ 蜂鸣器                                │  │
                                        │  └──────────────────────────────────────┘  │
                                        └──────────────────────────────────────────┘
                     闸杆 300×5×8 从 +X 侧伸出，转轴离地 42mm，杆顶 46mm
```

**方向约定**：`-X` = 前（长脚，放按键和 Type-C）；`+X` = 闸杆伸出方向（道路侧）；`Z` = 车辆行驶方向。**闸杆与按键分处闸体两侧，杆不会扫过按键。**

---

## 2. 主机 BOM

| 类别 | 型号 | 数量 | 选型理由 / 注意事项 |
|---|---|---|---|
| 主控 | **ESP32-S3-DevKitC-1 N16R8** | 1 | ESP-SR 模型分区要 6MB，PSRAM 是 AFE 硬性要求。底座内**平放**（板长 63mm > 内高 17mm，不能立放） |
| 麦克风 | INMP441（I2S MEMS） | 1 | 装闸体前面板内侧。整机无喇叭 → **v1 §7.4 的自唤醒问题不存在** |
| 电机 | **N20 减速电机 6V 15rpm** | 1 | 经 5V 驱动 ≈12.5rpm，90° 行程约 1.4s，接近真实道闸节奏 |
| 电机驱动 | **DRV8833** 模块 | 1 | 过流/过热保护 + nSLEEP 待机断电 |
| ~~电流采样~~ | ~~0.33Ω 1W 电阻 + 10kΩ/100nF RC~~ | ~~1~~ | ❌ **已删除**。成品 DRV8833 模块不引出 `AISEN`，电机电流不经过 `GND` 脚，采样读不到有效信号。改用**行程超时反转**，见 [网表基准 v2 §4/§5.1](道闸玩具_底板网表基准_v2.md) |
| 限位开关 | 微动开关**（必须带簧片杠杆）** | 2 | **常闭 NC 接法**（fail-safe）。由转毂凸轮压杠杆。~~短柄无杠杆~~ **已推翻**：凸轮升程 3mm，直动柱塞总行程只有 ~0.5mm，会被压毁，见 [基准表 §1](道闸玩具_元器件尺寸基准_v1.md) |
| 按键 | 6×6×**12** 轻触开关（底座 3.5 + 柄 8.5）**+ 打印按键帽 ×2** | **2** | 抬杆 / 落杆。复位 = 双键同时长按 3 秒 / 语音 / 遥控器 |
| 总电源 | SS12D00 拨动开关 | 1 | 底座前端面，串电池正极 |
| 蜂鸣器 | 有源蜂鸣器 3.3V，φ9×5mm | 1 | 取消爆闪灯后**唯一的反馈通道** |
| 电池 | **103450 锂聚合物 1800mAh**（10×34×50mm） | 1 | 18650 带座 21mm，底座内高只有 17mm 装不下。**带保护板的实际总长待实测**，可能超过电池仓的 52mm，见基准表 §3 |
| 充电 | **TP4056 Type-C 模块（带 DW01+FS8205 保护）** | 1 | 必须带保护板。**Rprog 换 2kΩ → ≈580mA**（0.32C），小空间发热可控，满充约 3.5h |
| 升压 | 5V 升压模块（SX1308 / MT3608） | 1 | 输出调 **5.1~5.2V**。去掉喇叭后峰值负载只剩 ~0.6A |
| 电容 | 470µF/10V 低 ESR ×1、220µF ×1、100nF ×3 | | 见 §3 |
| 其他 | 1N5819 ×1、100kΩ ×2、M2 自攻 ×12 | | 电源隔离 + 电池采样 |
| 机械件 | φ8 铜套 ×1、M2 顶丝 ×1、M5 钢螺母 ×2（配重，可选） | | |

**遥控器 BOM**：ESP32-C3 SuperMini ×1、6×6 轻触开关 ×3、**502030 锂电 250mAh**、TP4056 Type-C 模块 ×1、SS12D00 拨动开关 ×1。状态灯用板载 WS2812（GPIO8）。

> ⚠ **CAD 的遥控器外壳漏了两处**：没有拨动开关开口，也没有 WS2812 的灯孔（目前只有 1 个 Type-C 开孔 + 3 个按键孔）。另外遥控器**还没有原理图和 PCB**，选型锁定只覆盖底座承载底板。
>
> ⚠ **主机没有任何指示灯**——BOM 里一颗 LED 都没有，结构上也没有灯孔，TP4056 自带的充电灯被埋在底座里看不见。方案见基准表 §5。

### 相对 v1 的删除清单

| 删除 | 原因 |
|---|---|
| MAX98357A + 4Ω3W 喇叭 | 不做语音播报。**连带消除 v1 §7.4 的声学隔离难题** |
| 18650 + 电池座 | 20mm 高的底座装不下 |
| 红外对射管 | 对射需两侧立柱，与"装在底座"矛盾 |
| WS2812 闸杆灯带 | 灯带 10mm 宽，闸杆只有 5mm |
| **WS2812 爆闪灯 ×2 + 天窗 + 半透光片** | 用户取消 |
| **VL53L0X ×2 + 防砸使能开关** | 用户可选取消。见下方说明 |
| 第三个底座按键（复位） | 用户改为 2 键 |
| AMS1117 备用 LDO | 去喇叭后 DevKit 板载 LDO 余量充足 |

### 为什么不用侧装测距（原"堵转检测"论证已部分失效）

不是省钱的问题——VL53L0X 约 ¥9/只，两只才 ¥18。真正的原因是**覆盖范围**：传感器装在底座侧面，25° 视场角在 250mm 处只覆盖约 110mm 宽的锥形区，而闸杆有 **300mm** 长，杆中段和末端下方是盲区。而行程超时测的是"闸杆有没有按时到位"，同样**覆盖闸杆全长、没有盲区**——只是响应从毫秒级变成秒级（0.2~1.6s，压紧力约 1.3N）。

代价是失去"来车自动抬杆"（需要非接触检测）。GPIO 7/8/15/16 已预留，随时可以加回。

---

## 3. 电源链路

```
103450 LiPo 1800mAh (3.0~4.2V)
  │
  ├─► TP4056 + DW01/FS8205 保护  ◄── Type-C（底座前端面）
  ├─► 总电源拨动开关（底座前端面）
  ├─► 100kΩ ─┬─ 100kΩ ─ GND  → GPIO1 (ADC1_CH0) 电池采样，并 100nF
  │
  └─► 升压 5.1V ─┬─► 470µF + 100nF ─► DRV8833 VM
                  │                    DRV8833 GND ─► GND   (采样电阻已删除)
                  └─► 1N5819 ─► 220µF ─► DevKit 5V ─► 板载 LDO ─► 3.3V ─► INMP441 / 逻辑
```

v1 §4 的三重防护（大电容 / 二极管隔离 / PWM 软启动）全部保留。峰值负载从 v1 的 ~1.6A 降到 ~0.6A，电容可从 1000µF 降到 470µF。

### 功耗与续航

| 工况 | 电流 @3.3V |
|---|---|
| ESP32-S3 常态跑 ESP-SR（AFE+WakeNet+MultiNet） | ~110mA |
| 蜂鸣器（瞬时） | ~30mA |
| 电机峰值（瞬时 @5V） | ~400mA |
| **待机监听合计（折算到电池侧）** | **~115mA** |

1800mAh ÷ 115mA ≈ **15.6 小时连续监听**（取消 ToF 和爆闪灯后比 v2.0 提升约 30%）。

---

## 4. 引脚分配

**ESP32-S3-DevKitC-1 N16R8 禁用脚**：GPIO26~32（Flash）、GPIO33~37（八线 PSRAM）、GPIO19/20（USB）、GPIO43/44（UART0）、GPIO0/3/45/46（strapping）。

### 4.1 主机

| 功能 | 引脚 | 备注 |
|---|---|---|
| 麦克风 SCK | GPIO4 | INMP441，I2S0 |
| 麦克风 WS | GPIO5 | |
| 麦克风 SD | GPIO6 | L/R 脚接 GND |
| ~~电机电流采样~~ | ~~GPIO7~~ | ❌ 已删除，**GPIO7 空闲预留** |
| 电机 AIN1 | GPIO9 | LEDC PWM 20kHz |
| 电机 AIN2 | GPIO10 | LEDC PWM 20kHz |
| 电机 nSLEEP | GPIO11 | 高=工作，低=待机断电 |
| 限位·抬到位（90°） | GPIO12 | 内部上拉，NC 接法 |
| 限位·落到位（0°） | GPIO13 | 内部上拉，NC 接法 |
| 按键·抬杆 | GPIO17 | 到 GND，内部上拉 |
| 按键·落杆 | GPIO18 | 双键同时长按 3s = 复位 |
| 蜂鸣器 | GPIO2 | |
| 电池采样 | GPIO1 | ADC1_CH0，11dB 衰减 |
| 预留（可加回 ToF） | GPIO8/14/15/16/21/47 | I2C + XSHUT ×2 + 灯 |
| 备用 | GPIO38~42, 48 | |

### 4.2 遥控器（ESP32-C3 SuperMini）

| 功能 | 引脚 | 备注 |
|---|---|---|
| 按键·抬杆 | GPIO2 | C3 深睡唤醒只支持 GPIO0~5 |
| 按键·落杆 | GPIO3 | |
| 按键·复位 | GPIO4 | |
| 状态灯 | GPIO8 | 板载 WS2812 |
| 电池采样 | GPIO0 | 100k/100k 分压 |

---

## 5. 机械设计与核算

### 5.1 闸杆

- 截面 5(宽) × 8(高)，**空心，壁厚 1.2mm** → 截面积 25.4mm²
- 长 300mm，PLA 1.24g/cm³ → 质量 ≈ **9.4g**
- **分两段打印**：2×155mm，搭接榫 20mm + M2 螺丝 ×2，兼容 220mm 幅面打印机，也更耐摔
- **刚度**：I = 213mm⁴，自重悬臂端挠度 δ = wL⁴/8EI ≈ **0.58mm**，肉眼几乎看不出

### 5.2 扭矩

- 水平位最大静扭矩 = 9.4g × 15cm = **0.14 kgf·cm**
- N20 6V 15rpm 堵转约 2.5 kgf·cm，经 5V 驱动仍有 ~2.0 kgf·cm
- **安全系数 ≈14×**

### 5.3 90° 竖直位的两个新问题（v2.0 是 80°，不存在这些）

1. **过冲翻倒**：闸杆到 90° 时重力矩归零，若限位失效，惯性会带它越过竖直向后倒。
   → 闸体后侧设计**机械止挡筋**（在 **100°** 位置），微动限位是第一道、止挡筋是第二道保险。
   止挡块位置由**扫掠差集**算出（毛坯 − 转臂 0…100° 扫过的区域），不是手摆角度——手摆的话挡块会埋在转臂的扫掠包络里，闸杆 35~40° 就卡死。
2. **末端不减速就会撞止挡**：接近 90° 时重力不再帮忙减速。
   → 固件在行程 0.85T 之后把占空比降到 30%（比 v2.0 的 0.8T/40% 更早更低）。

### 5.4 配重

N20 减速箱不自锁，但 1:300 减速比下 0.14 kgf·cm 反射到电机端只有约 0.0005 kgf·cm，**减速箱摩擦足以驻停**，不会自己掉杆。

配重仓仍保留（转毂尾部，放 1-2 颗 M5 钢螺母，约 4-8g，力臂 22mm），用于让落杆更柔和。仓口 M2 螺丝封盖，**杜绝可脱落小件**。

### 5.5 倾覆核算

闸杆改为 +X 伸出后，150mm 底座的长脚在 -X 侧，正好**反向配平**：

- 倾覆力矩（绕底座后缘 X=+75）：9.4g × 100mm = **940 g·mm**
- 恢复力矩：底座塑料 45g×75 + 电池 35g×105 + 闸体总成 40g×50 ≈ **9050 g·mm**
- **安全系数 ≈9.6×**，稳定

### 5.6 转毂（一件三用）

一个 3D 打印件同时充当**轴套 + 凸轮 + 闸杆夹**，总长 22mm（CAD v2.3 重做过插槽，以 CAD 为准）：

| 段 | 尺寸 | 作用 |
|---|---|---|
| 内段 | φ16，长 11mm，**1 个**凸轮凸起 | 在闸体内压两个微动开关（两开关相隔 90°，单凸起随转毂转动依次压下；**2 个凸起是错的**，会在 0° 时同时压下两个） |
| 轴颈 | φ8，长 4mm | 穿闸体 Z=-23 侧壁的 φ8 铜套 |
| 外段 | 16×14×12 夹块，5×8 插槽 + M2 螺丝 | 夹闸杆根部 |
| 中心孔 | φ3 D 形，深 9mm + M2 顶丝 | 直接夹 N20 的 D 形输出轴 |

N20 输出轴伸出长度与 D 面对边尺寸**待实测**（本文原写 10mm，CAD 按 9mm 建、孔深 10.5mm）。电机端面距凸轮 0.5mm。**不需要额外传动轴、联轴器或齿轮。**

> D 形孔必须是 **圆 ∩ 半空间**（把圆切掉一块平面），不能写成『圆 ∪ 方槽』——那样孔比轴还大，D 面失效，轴会空转。

---

## 6. 结构参数表（可直接建模）

### 6.1 底座

| 项 | 值 |
|---|---|
| 外形 | 150(X) × 55(Z) × **22**(Y)，X 从 -75 到 +75（v2.5 从 20 加高，为让低背排母装得下） |
| 壁厚 / 底厚 / 顶板 | 2.0 / 2.0 / 2.0mm |
| 内腔 | 146 × 51 × **17**（中央区；止口环下方只有 15） |
| 分件 | 下托盘 + 上盖板，M2 自攻 ×4 |
| 按键孔 | **φ9 ×2 + 背面 φ12.5×1.5 沉孔**，位置 (−40, +10) / (−20, +10)。配打印按键帽，按压面 φ8.5、露出盖面 2.5mm |
| Type-C 开孔 | **前端面 X=-75**，10×5mm，Y=8 |
| 总电源开关 | **前端面**，8×4mm，Z=+15。⚠ SS12D00G3 是拨柄朝上的立式开关，装底板上拨柄指天，与前端面开口**方向对不上**，见基准表 §1 |
| 压重仓 | X=-62 处 30×40×8 凹槽，M2 盖板 |

### 6.2 闸体

| 项 | 值 |
|---|---|
| 外形 | 46(X) × 46(Z) × 62(Y)，坐底座顶面 Y=22 → **顶面 Y=84，纯净无开孔** |
| X 位置 | X = +2 ~ +48 |
| 壁厚 | 2.0mm |
| 转轴中心 | X=+25，**Y=42**，轴线沿 Z，转毂从 **Z=-23** 侧壁伸出 |
| 闸杆姿态 | 落杆位：杆底 **Y=38**，杆顶 **Y=46**，沿 **+X** 伸出 |
| 起杆角度 | **90°**，凸轮限位 + **100° 机械止挡筋**（91° 离 90° 只差 1°，打印公差就能顶上；且止挡位置现由扫掠差集算出，不再手摆角度） |
| 麦克风格栅 | 前面板 X=+2，Y=60，φ1.8 ×4 通孔，背面留声腔。⚠ **CAD 里没有任何麦克风固定结构**，模块目前悬空，固定方式待拍板 |
| 分件 | 前壳 + 后壳，M2 自攻 ×4 |

### 6.3 闸杆

| 项 | 值 |
|---|---|
| 截面 | 5(Z) × 8(Y)，壁厚 1.2mm 空心 |
| 总长 | 300mm = 155 + 155，搭接 20mm |
| 分色 | 红/白交替，每段 37.5mm |
| 根部 | 20mm 实心，插入转毂夹块，M2 横穿 |

### 6.4 遥控器

| 项 | 值 |
|---|---|
| 外形 | 60(X) × 30(Z) × 20(Y)，四角 R4 |
| 内腔 | 56 × 26 × 16 |
| 按键 | φ6 ×3，顶面 X = -18 / 0 / +18 |
| Type-C | 一端面，10×5mm |
| 内部布置 | C3 SuperMini(22.5×18) 与 TP4056(26×17) 并排，电池 502030 垫底 |

---

## 7. 固件

### 7.1 状态机

```
BOOT → HOMING（慢速向"落"走，8s 未触限位 → FAULT）
         ↓
      CLOSED ⇄ OPENING → OPEN(90°) ⇄ CLOSING → CLOSED
                                         ↓ 电流超阈 80ms 或超时
                                    立即反向 → OPENING（防砸）
任意态 ──故障──► FAULT（断电机、连续短鸣、复位恢复）
复位入口：语音"复位" / 遥控器复位键 / 底座两键同时长按 3s
```

### 7.2 FreeRTOS 任务

| 任务 | 优先级 | 职责 |
|---|---|---|
| `sr_task` | 5 | AFE + WakeNet + MultiNet，独占一核 |
| `motion_task` | 6 | 状态机、PWM、限位、**行程计时与超时判定** |
| `comms_task` | 4 | ESP-NOW 收发与配对 |
| `input_task` | 3 | 按键消抖、双键长按复位、电池采样、蜂鸣器 |

所有任务只往 `evt_queue` 投事件，**`motion_task` 是唯一能命令电机的地方**（保留 v1 的规则，避免语音和遥控抢电机）。

### 7.3 电机控制

- PWM 20kHz（低于此值电机啸叫刺耳）
- 软启动：占空比 40% → 目标值，200ms 线性斜升
- **末端缓停：0.85T 之后降到 30% 占空比**（比 v2.0 更早更低，因为 90° 位重力不再帮助减速）
- 到位刹车：AIN1=AIN2=高 保持 300ms，再拉低 nSLEEP 断电
- **防砸**：行程超时 —— 落杆超 1.15×T 未到位 → 反向 + 蜂鸣；超 1.5×T → FAULT 断电

### 7.4 语音（ESP-SR）

| ID | 拼音 | 动作 |
|---|---|---|
| 1 | `qi gan,tai gan,kai men` | 起杆到 90° |
| 2 | `luo gan,jiang gan,guan men` | 落杆 |
| 3 | `fu wei` | 复位 |

唤醒词用乐鑫现成的（自定义唤醒词是付费商业服务）。整机无喇叭，**不需要 v1 的 disable_wakenet 开关逻辑**。

### 7.5 Flash 分区（16MB）

| 分区 | 大小 | 内容 |
|---|---|---|
| nvs | 24KB | 配对 MAC、配置 |
| app | 4MB | 固件 |
| model | 6MB | WakeNet9 + MultiNet7 中文（无 esp-tts） |
| ota | 4MB | OTA 备份 |
| 其余 | ~2MB | 预留 |

### 7.6 ESP-NOW

沿用 v1 的 12 字节 `gate_pkt_t` 协议与配对流程，命令码收敛为 `CMD_OPEN=1 / CMD_CLOSE=2 / CMD_RESET=3 / CMD_PING=7`。双方固定信道 1，主机不连路由器。

---

## 8. 实施路线

| 阶段 | 目标 | 完成标志 |
|---|---|---|
| **M0** | 环境 | ESP-IDF 装好，S3 跑通 hello_world，PSRAM 识别 |
| **M1** | 电机 | **USB 供电**下控制 N20 正反转，触限位自动停 |
| **M2** | 状态机 | 2 按键完整走 落→抬(90°)→落，含软启动、缓停、超时、双键复位 |
| **M3** | 防砸 | 标定单程行程时间 T；手挡闸杆能在 1.15×T 触发反转（延迟 0.2~1.6s） |
| **M4** | 遥控器 | C3 → ESP-NOW → 主机响应；配对可用；深睡实测 <20µA |
| **M5** | 语音 | ESP-SR 跑通，3 条命令识别率可接受（**最难一步**） |
| **M6** | 结构 | 打印全部零件，装配，验证转毂/限位/90°止挡/闸杆刚度/倾覆 |
| **M7** | 电池装机 | 切到锂电，验证电机启动不复位；整机联调 |

**M1 铁律：必须用 USB 供电。** 电池+升压的电源问题和电机逻辑问题混在一起排查会非常痛苦。

---

## 9. 采购分批

- **第一批（M0~M3）**：ESP32-S3-DevKitC-1 N16R8、N20 6V 15rpm（顺便买个 30rpm 备选）、DRV8833 模块、微动开关 ×5、0.33Ω 1W 电阻 ×5、面包板+杜邦线
- **第二批（M4~M5）**：ESP32-C3 SuperMini ×2、INMP441、有源蜂鸣器
- **第三批（M6~M7）**：103450 锂电、502030 锂电、TP4056 Type-C 保护板 ×2、5V 升压模块、470µF/220µF、1N5819、拨动开关 ×2、φ8 铜套、M2 螺丝包
- **结构件**：嘉立创三维打印。闸体黄色，闸杆红白，底座深灰

---

## 参考

- [ESP-SR 命令词 MultiNet](https://docs.espressif.com/projects/esp-sr/en/latest/esp32s3/speech_command_recognition/README.html)
- [ESP-SR 唤醒词 WakeNet](https://docs.espressif.com/projects/esp-sr/en/latest/esp32s3/wake_word_engine/README.html)
- [esp-sr GitHub](https://github.com/espressif/esp-sr)


---

## 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v2.1 | 2026-08-10 | 依据需求书 v2.1 的完整方案 |
| v2.5 | 2026-08-12 | 按键定为 6×6×12（原写 5，差 8.4mm）；**新增打印按键帽 ×2** —— 加高底座后 12mm 顶杆仍差 1.4mm 够不到上盖，且 φ3.5 裸顶杆儿童按不舒服。按键帽按压面 φ8.5、**露出盖面 2.5mm**；上盖孔 φ6.5→φ9 并加 φ12.5×1.5 背面沉孔做导向与防脱 |
| v2.4 | 2026-08-12 | 限位开关由"短柄无杠杆"更正为**必须带簧片杠杆**（凸轮升程 3mm vs 直动行程 0.5mm，量级不匹配） |
| v2.3 | 2026-08-12 | **同步网表 v2 的防砸方案变更**：删除电流采样电路（成品模块不引出 AISEN），改行程超时反转；GPIO7 释放为空闲 |
| v2.2 | 2026-08-12 | 与 CAD v2.6 对齐，修正 10 处不一致：凸轮凸起 2→1、止挡角度 91→100、底座高 20→22、内腔高 16→17、闸体顶面 82→84、转毂总长 24→22、内段 8→11mm；轻触开关高度与 N20 轴长标为待实测；补上拨动开关方向、麦克风无固定结构、遥控器缺开关孔/灯孔、主机无指示灯四处缺口。**物理尺寸的权威移交给《元器件尺寸基准 v1》。** |
