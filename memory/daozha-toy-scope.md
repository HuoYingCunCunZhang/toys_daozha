---
name: daozha-toy-scope
description: 道闸门玩具项目 —— 锁定决策、当前进度（元件已全部定案/PCB待做/CAD冻结v2.6）、以及四条踩坑教训
metadata:
  node_type: memory
  type: project
---

道闸（barrier gate）造型的儿童玩具，嘉立创打样 + 三维打印。目录 `D:\玩具\01.道闸门\`。

## 入口

**项目根 `README.md` 是总索引**，里面有文档清单、当前进度、和**给接手 agent 的 4 个任务**（改 EDA 封装 → PCB 布局 → 解冻 CAD 改 12 项 → 遥控器电路）。接手时先读它。

`道闸玩具_元器件尺寸基准_v1.md` 是**所有物理尺寸的唯一事实源**，CAD 和 PCB 都只能引用它，不许自己定义元件尺寸。

## 工具环境

- OpenSCAD 2021.01 在 `F:\Program Files\OpenSCAD\openscad.exe`（不在 PATH）
- **OpenSCAD 在中文路径下无法渲染** → 工作副本放 `D:\cad\`，改完同步回 `cad\`
- 命令行用数字选择器 `-D p=N`，别用 `-D part="x"`（PowerShell 吞引号）
- 嘉立创EDA 桥接已跑通，见 `easyeda-api` skill 与 [[easyeda-bridge-setup]]

## 进度（2026-08-13 晚）

需求/电路方案/元器件选型尺寸 ✅ → 改 EDA 封装 ✅ → **PCB 布局 v3 定稿 ✅**（按可布线性重排，总 HPWL 1711→1010mm，改善 41%）→ **PCB 布线 🔴 下一步** → CAD ⏸ 冻结 v2.6 → 固件 ⏸ 未开始。

**布局方法（有效，下次照做）**：读出每个焊盘的网络+坐标，发现 **U1 有 14 个信号挤在同一排**，于是让外围件的左右次序严格对齐这排引脚的次序（J2限位→U2电机→按键→J3麦克风），飞线基本不交叉。用 **HPWL**（各网络包围盒半周长之和）量化前后差异。**贴片矮件放模块肚子底下**（排母架空 7.5mm），大幅缓解拥挤；11mm 高的电解电容不行。

🔴 **板子和底座已放大**：承载底板 100×55 → **120×65**，底座 163×63×25 → **183×73×25**。根因是 U4 实测 37mm 长（定 100×55 时按 SX1308 的 23mm 算），37mm 连续跨度在小板上差 3.2mm 放不下。**CAD 要按新底座全面重算**，元件坐标全在机械约束 §七之二。按键位置统一为 X=−50/−30（两份文档原先对不上）。

🔴 **系统库的 DevKit 封装行距是错的**：`CONN-TH_ESP32-S3-DEVKITC-1U-N8` 给 22.86mm，实际 **25.4**（卖家尺寸图上 25.4=行距、27.94=板宽）。照库打样插不进去。已自建 `_S25.4` 修正版 + 器件 `ESP32-S3-DevKitC-1-N16R8-行距25.4`。**换器件时沿用同一符号，引脚坐标不变，20 根导线一根没动。**

四项：删 R6+`M_FAULT`（GPIO38 释放，网络 **25→24**，未连接引脚 31→41）、SW3 换 SK-12D07VG7（系统库现成，LCSC C49284740）、U2 换自建 2×8 排母座（排距 **15.24**）、U4 换自建 MT3608 四角焊盘（孔距实测 **30×7**，孔 φ1.651）。

**U2/U4 是自建器件，存在个人库**（`198943fe383c4dc5ab0bb8a89b65edd5`），系统库里只有它们的裸芯片、没有模块。**U2 焊盘是按列编号**（1-8 左列 / 9-16 右列），不是常规奇偶交替——布线前必看选型锁定文档里的对照表，照编号连会全错。

**MT3608 实物是 37 × 17，不是文档一直写的 36**（实物尺寸图，重 4.39g）。正极排布已确认："+"走一条长边、"−"走另一条，封装编号无需调整。
~~⚠ 遗留：工程库封装副本可能仍是 36~~ → **已确认工程库副本确实是旧的 36×17，已在 PCB 里把那 4 根丝印线直接改成 37×17**。（「更新」只刷新器件名，不换封装副本。）

🔴 **PCB 布局当前卡点：U1 DevKit 尺寸三个来源打架** —— 排针行距 文档 25.4 vs 库封装 **22.86**（行距错了根本插不上）；板长 文档 63.611 vs 包围盒 69.34。U3 占 28.3，U1 若 63.5 则 +X 边还剩 8.2mm 放端子，若 69.3 就没地方。**要用卡尺量 DevKit 板长/板宽/两排排针中心距。**

⚠ **`getPrimitivesBBox()` 把位号文字也算进去**，不能当实体尺寸用（U2 丝印 18.1 宽，包围盒报 21.08）。算净距要用实体尺寸。

⚠ U2 排距文档写 15、我按 **15.24** 建（8 脚必落 0.1″ 栅格）。文档原来"用椭圆焊盘吸收 15/15.24 差异"的说法推理不成立——模块靠刚性排母插着，板上焊盘形状影响不到能否插入，那 0.24 是排母插孔顺应量吃掉的。

## 锁定决策

- **语音是核心**：ESP32-S3-DevKitC-1 **N16R8** + ESP-SR，仅 3 条命令（起杆/落杆/复位）。**无喇叭/无TTS/无录音回放**
- **无爆闪灯、无闸杆灯带**；指示灯方案 = **在模块自带灯正上方开观察孔**（底座对 TP4056 充电灯，遥控器对 C3 板载蓝灯）
- **防砸 = 行程超时反转**（落杆超 1.15×T 反转、超 1.5×T 进 FAULT）。⚠️ 曾定"堵转电流检测"，**已推翻**——成品 DRV8833 模块不引出 `AISEN`，电机电流不经过 `GND` 脚，电阻串 GND 只能读到几 mA 静态电流。代价：响应从毫秒级变**秒级（0.2~1.6s）**、压紧力约 1.3N
- 玩家 6~10 岁
- **尺寸**：底座 **163×63×25**，承载底板 **100×55**（换算 `BASE_L=板长+63`、`BASE_W=板宽+8`）；闸体 46×46×62；闸杆 300×5×8 **从按键相反侧(+X)伸出**；**转轴离地 42、杆顶 46**（用户小车最矮 50，必须低于它）；起杆 90°；底座 2 键；Type-C + 总开关在**前端面**；遥控器 60×30×20 三键
- **转毂拆成 `hub`（主体，一整件）+ `cam`（凸轮盘）** —— 拆件与电机固定无关，是壁孔 φ8.5 卡住了 φ18.6 凸轮和 36×14 臂。拆点选凸轮而非臂：凸轮不传扭矩不受弯，主体保持整体承力
- **电机只靠矩形隧道夹紧**，不做 M1.6 支架板（端面 M1.6 孔留作日后加固）

## 采购卡点（买错就装不上）

- **排母只买低背 5.0mm**，常见的 8.5mm 装不下
- **限位开关必须带簧片杠杆**（ZIPPY DF-P1S-1E-M-Z），无杠杆直动式行程 0.3mm 会被 3.3mm 凸轮升程压毁
- **轻触开关 6×6×12，动作力 100~160gf**，别买 300gf
- **拨动开关 SK12D07VG7**（卧式侧拨），不是立式的 SS12D00G3
- **MT3608 买 36×17 那款**（无 USB 座），SX1308 高 12mm 装不下

## 四条教训（都是真金白银换的）

1. **`Volumes=2 / Simple=yes` 只证明单件几何合法，完全不证明能装配。** v2.2 九件全过，用户仍发现 4 处装不上的硬伤。v2.3 起改用**布尔交集自检** `cad\check.scad`，并且**必须带阳性对照**（故意摆错必须非空），否则检测失效了都不知道。**但它测的是终态，测不出装配路径** —— 转毂穿不过壁孔就是自检抓不到的。
2. **模块叠高要一层层数。** DevKit = 排母 + **公排针塑料隔片 2.5** + 板 1.57 + 模组 3.1，四层。漏掉隔片那层就是 2.5mm，直接决定方案成不成立。
3. **转动件的限位不能靠"把挡块摆到那个角度"。** 挡块要待在转动件的**扫掠包络之外**，包络由"侧面到转轴的距离"决定而非角度。用**扫掠差集**生成位置，且**相邻角度实例必须取 `hull()` 填缝**——只做并集会在尖端半径漏出细缝，变成提前挡路的毛刺。
4. **几何"看起来对"不等于对。** D 形孔曾被写成 圆 **并** 方槽（应为 圆 **交** 半空间），渲染图上看不出来，实际轴插进去空转。这类问题只能用阳性对照钉死。

**另加一条方法论**（2026-08-12 用户提出，已成为项目原则）：**先定元件尺寸，再动 3D。** v2.2~v2.6 连续四轮修的硬伤根因是同一类——拿没核实的元件尺寸建模。同理，**卖家详情页的"行程"是典型值，官方规格书的 `PT Max` 才是设计依据**（这条差点让一批限位开关全部失效）。

## 其他

- **Gmail 连接器权限不足**，无法建草稿/发邮件（用户邮箱 332437232@qq.com）
- 用户是**第一次做硬件**，需要清晰的操作流程 —— `道闸玩具_采购与装配清单_v1.md` 就是为此写的（A类嘉立创贴片/B类自己焊/C类模块机械件 + 六阶段装配流程）
