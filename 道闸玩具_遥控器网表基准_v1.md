# 道闸玩具 · 遥控器 网表基准 v1

> 依据：`道闸玩具_电路与结构设计方案_v2.md` §4.2、`道闸玩具_元器件尺寸基准_v1.md`
> **用途**：画原理图 / PCB 之前的"正确性基准"。画完后用 EDA API 读回实际网表，与本文逐条比对。
> 主机那块板就是这么做的（24 条网络逐条比对），**别跳过这一步**。

2026-08-15 用户拍板三条，直接砍掉了一半电路：

| 决定 | 后果 |
|---|---|
| **不做状态灯** | 不用 WS2812 / 不用板载 LED，上盖不开灯孔。顺带了结了"WS2812 还是普通 LED"的文档打架 |
| **不做电量检测** | 删掉 100k/100k 分压 —— 它静态就吃 **18.5µA**，而深睡指标 <20µA，遥控器只有 250mAh。**BOM 里一颗电阻都不需要了** |
| **外壳尺寸可动** | 优先保证装得下，见尺寸基准 §遥控器内腔核算（目标 68 × 32 × 22）|

---

## 1. 板子定位

**载板**：现成模块（C3、TP4056）焊在板上，三个按键和拨动开关也焊在板上，电池垫在板底下。

- 板子外形**由布局决定**，不预先框死 —— 外壳跟随板子（项目原则：结构让路给 PCB）
- **元件高度预算：板面以上 ≤ 6.9mm**。最高的是 C3 的 USB-C 座（叠高 6.7）
- 🔴 **不能用 KF128 接线端子**：它高 9.5mm，直接超预算。电池引线**焊在板上的两个焊盘**上

---

## 2. 元件位号表

| 位号 | 器件 | 说明 |
|---|---|---|
| U1 | **ESP32-C3 SuperMini** | 主控，22.52 × 18 |
| — | **2.54 低背排母 5.0mm，1×8 ×2** | 🔴 **给 U1 用，必须低背 5.0**（同主机）。模块要反复拔下来改程序，**不能直焊** |
| U2 | **TP4056 Type-C 模块（带 DW01+FS8205 保护）** | 28 × 17，充电口就是它的 Type-C |
| SW1 | 6×6 轻触开关 | **抬杆** |
| SW2 | 6×6 轻触开关 | **落杆** |
| SW3 | 6×6 轻触开关 | **复位** |
| SW4 | **SK12D07VG7 拨动开关**（卧式侧拨） | 总电源。🔴 不是立式的 SS12D00 |
| C1 | 10µF / 16V **0805 陶瓷** | V5 轨储能，**贴板底面**。见 §4.3 |
| P1 / P2 | 焊盘（非端子） | 电池 +/− 引线 |

**轻触开关高度待定** —— 要等板面 z 定了才能选，见 §5.2。

### EDA 选型锁定（2026-08-15 已建工程并连线）

工程 **`道闸玩具遥控器`**，uuid `ea4e1a7afdae48fc87c730f80a6c9114`（Board1 / Schematic1 / PCB1）。

| 位号 | 器件名 | 封装 | device uuid |
|---|---|---|---|
| U1 | **ESP32-C3 SuperMini_TH** | `COMM-TH_ESP32-C3-SUPERMINI_TH` | `1fc07a0a91f44aafa9d865ca6eb59e38` |
| U2 | TP4056充电源模块板(Type-C) | `PWRM-TH_TP4056` | `3aa84456264e47708e126cdf52ec3c68` |
| SW1/2/3 | 轻触开关6X6X12 | `SW-TH_4P-L6.0-W6.0-P4.50` | `00ec1270e33944788eff4adebd5b6ca7` |
| SW4 | SK-12D07VG7 | `SW-TH_SK-12D07VG7` | `134d2d15e32949759c530cbf1dbeca16` |
| C1 | CL21A106KAYNNNE_C15850（10µF 25V）| `C0805` | `8c1f4a9e9d204b148b36ce719c891bd1` |
| P1 | HEAD2（1×2 P2.54）| `HDR-TH_2P-P2.54-V-M` | `75deb6b8f00547acbb71a567c2dd03b2` |

**U1 选直插版（`_TH`）不是贴片版**：库里两个都有（贴片版 `70fbe3e829f842f390c40919da2703fb`）。

### 🔴 U1 必须插排母，不能直焊（2026-08-15 用户确认）

模块要反复拔下来改程序。**封装不用改** —— 16 个 2.54 通孔、行距 15.24，排母直接焊进同一组孔，模块再插上去。但叠高变了：

| | 直焊 | **插低背排母 5.0** |
|---|---|---|
| 排母本体 | — | 5.0 |
| 模块排针塑料隔片 | 2.5 | 2.5 |
| 模块 PCB | 1.0 | 1.0 |
| USB-C 座 | 3.2 | 3.2 |
| **合计** | 6.7 | **11.7** |

**外壳因此从 22 长到约 26 高。** 顺带一个好处：模块被架空 **7.5mm**，天线离铜箔远了，射频条件反而改善（主机的 DevKit 就是这个情况）。

⚠ **按键跟着受影响**：板面 11.1、盖面 26 → 顶杆要够到 **14.9mm**。6×6 轻触最高只到 13 左右，**必须配打印按键帽**，和主机一样。

**P1 用 1×2 排针封装当焊盘用**：不装排针，电池引线直接焊进两个孔。

**不能用 KF128 端子**——它高 9.5mm，而板面以上原本只有 6.9mm 的预算（插排母后虽然涨到 11.7，但那是给 U1 的，电池焊盘该矮还得矮）。

### ✅ 封装几何已核对（2026-08-15，转 PCB 后从 PCB 文档读焊盘坐标）

系统库封装读不了几何（`lib_Footprint.get` / `.copy` 都抛不透明错），所以是**导入 PCB 之后**从焊盘坐标反算的：

| 封装 | 读回值（相对元件中心） | 判定 |
|---|---|---|
| **U1** `COMM-TH_ESP32-C3-SUPERMINI_TH` | 两排焊盘 x = ∓7.62 → **行距 15.24**；每排 8 脚 P2.54，跨距 17.778 | ✅ **用户卡尺实测确认 15.24**。板宽 18 → 每排距板边 1.38mm，几何自洽 |
| SW4 `SW-TH_SK-12D07VG7` | 信号脚间距 **2.0**、固定脚总跨 **8.3** | ✅ 与规格书一致 |
| SW1-3 `SW-TH_4P-...-P4.50-LS6.1` | x 跨 **6.10**、y 跨 **4.50** | ✅ 并证实**左右两列各自内部导通**（1/3 一侧、2/4 另一侧），与"脚1=信号、脚2=GND"的接法一致 |
| C1 `C0805` | 焊盘间距 2.0 | ✅ |
| P1 `HDR-TH_2P-P2.54-V-M` | 2.54 | ✅ |
| U2 `PWRM-TH_TP4056` | 焊盘跨 23.45 × 14.36（本体 28×17，四周内缩合理）| ✅ |

> **主机栽过的坑在这里没有重演**：DevKit 那次系统库给 22.86、实物 25.4，照库打样插不进去。
> 这次 15.24 = 0.6 英寸，是 DIP 标准值，且与 18mm 板宽自洽；用户实物量过，闭合。

---

## 3. 网表（8 条网络）

| # | 网络名 | 连接点 |
|---|---|---|
| N1 | `VBAT` | 电池+ (P1) → U2.**B+** |
| N2 | `VBAT_N` | 电池− (P2) → U2.**B−** |
| N3 | `VSYS` | U2.**OUT+** → SW4.公共脚（中间脚）|
| N4 | `V5` | SW4.常通脚 → U1.**5V**，C1.1 |
| N5 | `GND` | U2.**OUT−**，U1.**GND**，SW1.b，SW2.b，SW3.b，C1.2 |
| N6 | `BTN_UP` | SW1.1 → U1.13 = **GPIO4** |
| N7 | `BTN_DN` | SW2.1 → U1.12 = **GPIO3** |
| N8 | `BTN_RST` | SW3.1 → U1.10 = **GPIO1** |

> **为什么是 4/3/1 而不是原计划的 3/4/5**：库里 SuperMini 符号把 GPIO0~4 放在**右侧一列**（引脚 9~13），而 **GPIO5 在左侧**（引脚 1）。三个按键都在右边，用 GPIO5 就要绕过整个符号，必然与电源线交叉。
> 右列避开 strapping 脚 GPIO2 之后可用的是 GPIO0/1/3/4，取 **4/3/1**。约束一条没破：**三个都在 GPIO0~5 内（深睡唤醒要求），且都不是 strapping 脚**。

### 🔴 N2 不是 GND，别接在一起

`VBAT_N`（电芯负）和 `GND`（系统地）之间**隔着 DW01 控制的两颗保护 MOS**（FS8205）。
把它们连起来 = **把保护电路短路掉**，过放/过流/短路保护全部失效。

TP4056 带保护模块的四个端子就是为此分开的：**B+/B− 接电芯，OUT+/OUT− 接负载**。
（不带保护的模块只有 BAT+/BAT−，那种不能用。）

---

## 4. 关键设计说明

### 4.1 三个按键都必须落在 GPIO0~5，且不能用 GPIO2

- **GPIO0~5**：ESP32-C3 只有这几个脚支持**深睡唤醒**。遥控器平时深睡，靠按键唤醒
- **GPIO2 是 strapping 脚**，上电瞬间必须为高。按键按下是拉低的 —— 小孩按着"抬杆"键去拨电源开关，芯片可能进不了正常启动模式
- 避开 GPIO2 后可用的是 GPIO0/1/3/4/5，取 **3/4/5** 三个连号，顺带离 GPIO8/9（另两个 strapping 脚）也远

**按键一律"按下接 GND"，用 C3 内部上拉，不要外部电阻。**

### 4.2 拨动开关必须串在电池和 C3 之间

```
电池 ──► U2 的 B+/B−（充电与保护）
             │
        OUT+/OUT−
             │
        SW4 拨动开关（只断正极）
             │
        U1 的 5V 脚 ──► 板载 LDO ──► 3V3
```

**位置不是随便挑的，它同时解决烧录时的倒灌**：

C3 SuperMini 板上的 Type-C（烧录口）5V 是直通 5V 脚的。如果开关不在这个位置，插着 USB 烧录、电池又接着，USB 的 5V 会顺着 5V 脚倒灌回 U2 的 OUT+ —— 而 **OUT+ 与 B+ 直连**（保护只切负极），等于拿 5V 直接怼电池。DW01 会在 4.28V 跳过充兜底，但那是靠保护救命，不是设计。

开关串在中间之后：**关掉开关 = 电池与 C3 完全断开**，倒灌路径不存在。

> **规矩：烧录前先把拨动开关拨到关。** 充电不受影响 —— 充电走 U2 自己的 Type-C 到 B+/B−，与开关无关。

**进 5V 脚而不是 3V3 脚**：让板载 LDO 稳压。电池 4.2→3.0V 掉到 LDO 压差以下后输出跟着电池走，而 C3 本来就能在 3.0V 工作。

### 4.3 C1 为什么建议加

ESP-NOW 发射瞬间电流会跳到上百 mA，而供电路径上串着电池引线 + 拨动开关 + 排针，都有寄生电感。C3 SuperMini 板上自带的输入电容不一定够，最坏结果就是主机那份文档里写过的同一个症状——**一按遥控器就重启**。

一颗 10µF 0805 陶瓷，成本几分钱，**贴板底面不占高度预算**。
⚠ **不要用电解电容**：φ5×11 的立式电容高 11mm，板面以上只有 6.9mm，直接装不下。

---

## 5. 布局要点（画 PCB 时看这一节）

### 5.1 🔴 C3 的天线端必须悬出板边

SuperMini 的天线在板子一端。天线正下方压着铜箔会严重削弱 ESP-NOW 距离。
**要么让天线端悬出载板边缘，要么天线正下方整片挖空**（无铜、无走线、无地平面）。

主机那边不明显（DevKit 插排母架空 7.5mm），遥控器贴板直焊就非常要命。

### 5.2 轻触开关高度要等板面 z 定了再选

板面 z ≈ 11.1、盖面 z ≈ 22（按目标外壳 68×32×22 算）→ **顶杆要够到 10.9mm 才与盖面齐平**。

主机就是漏了这条：6×6×12 的顶杆差 7.4mm 够不到盖面，最后不得不补两个打印按键帽。
遥控器现在还来得及**直接选对高度**（6×6 有 4.3/5/7/8/9/10/12mm 等档），选不到合适的就同样配打印帽。

**动作力选 100~160gf**，别买 300gf 的——6 岁小孩按不动（这条主机已验证）。

### 5.3 三个按键的间距要按手指定，不是按板子定

顶面三个键，中心距 **≥ 18mm**（主机是 20mm）。φ9 级别的按键帽/按键孔，间距小于 15mm 手指会误触。

### 5.4 其他

| 项 | 要求 |
|---|---|
| U2 的 Type-C | 朝板子一端，对准外壳端面开口 |
| SW4 拨柄 | 同一端面，让开 Type-C |
| 电池焊盘 P1/P2 | 靠近 U2 的 B+/B−，引线短 |
| 板底面 | 只有 C1（0805）和电池，**电池垫在板下**，注意支撑柱高度 |

---

## 6. 校验结果 ✅（2026-08-15）

原理图已连完并保存，用坐标几何重建网表，**8 条网络与 §3 逐条对上**：

```
[C1.2, SW1.2, SW2.2, SW3.2, U1.15, U2.4]   GND
[C1.1, SW4.3, U1.16]                        V5
[SW4.2, U2.1]                               VSYS
[P1.1, U2.2]                                VBAT
[P1.2, U2.3]                                VBAT_N
[SW1.1, U1.13]                              BTN_UP  (GPIO4)
[SW2.1, U1.12]                              BTN_DN  (GPIO3)
[SW3.1, U1.10]                              BTN_RST (GPIO1)
```

**未连接的引脚全部是预期内的**：U1 的 11 个空闲 GPIO、U2 的 IN+/IN−（模块自带 Type-C，不外接）、SW4.1（SPDT 用不到的另一个触点）、SW4.4/5（固定脚）、三个按键的 3/4 脚（与 1/2 脚在开关内部同点）。

**阳性对照（两条都通过）**：

| 假引脚 | 位置 | 结果 |
|---|---|---|
| `FAKE.A` | (5,5)，远离一切 | 落单 ✅ |
| `FAKE.B` | (910,495)，**距 SW1.1 只有 5 个单位** | 掉出 BTN_UP ✅ |

第二条是关键：它证明校验器**真的在判别距离**，而不是把附近的点一律并进去。没有这条，工具失效了也看不出来。

### 方法（照主机那套）

1. 读回所有元件引脚坐标 + 所有导线几何（`getState_Line()` 返回的是**按段展开的扁平数组**，每 4 个数一段）
2. 并查集：每段两端点合并 → 落在任意线段上的点（含 T 型接点）并进去
3. 与 §3 逐条比对
4. ⚠ **必须带阳性对照**，否则工具本身失效了都不知道

### 另加一条：导线有没有压在符号上

网表对 ≠ 图画得对。把每根导线段与每个元件的**引脚包围盒**求交，看有没有线从符号肚子里穿过去。

这次就抓到一处：C1 的 GND 支线原本走 y=300，正好从 SW3（引脚盒 y 270~310）中间穿过。**它不短路**——网表校验已证明 SW3 三个网络都对——但线压在符号上是隐患：以后谁挪一下元件就可能变成真短路。已改走 y=300 → x=860 → 下到主干 y=240 绕开。

> 这条检查很便宜（几十行），而且**是纯几何的、不用看图**。改完记得重跑网表校验，别只跑一个。

> ⚠ `sch_Net.getAllNetsName()` 返回**空**，导线的 `getState_Net()` 也是空 —— 这是主机那次就发现的 API 退化，**网表只能靠坐标几何重建**，不能信 EDA 自己报的网络名。
> ⚠ **网络标识（GND/5V 符号）放不进去**：`sch_PrimitiveComponent.create` 放 netflag 类型必定卡 30 秒超时（EDA 大概率弹了命名框等点击）。本图因此**全部用普通导线连通、零交叉**，不依赖任何网络标识。

---

---

## 6b. PCB ✅（2026-08-15，DRC 零错误）

**板 57 × 33**（x ±28.5，**y −18 … +15**），双层。板框画在 Board Outline 层（11）。
y −18…−15 那条是**纯安装边**，为了给锁紧螺柱腾缺口，见 §6c。

| 位号 | 坐标 (mm) | rot | 说明 |
|---|---|---|---|
| U2 TP4056 | (−13.5, −5.5) | 0 | Type-C 朝 −X，本体前沿距板边 0.5 |
| U1 C3 SuperMini | (16, −5.5) | **270** | 长边沿 x，Type-C 朝 +X 板边，天线端朝 −X。⚠ **外壳 +X 端不开孔**，见 §6c 末 |
| SW4 拨动 | (−23.1, 9.5) | **270** | 🔴 见下 |
| SW1 抬杆 | (−16, 9) | 0 | |
| SW2 落杆 | (2, 9) | 0 | 三键间距 18mm |
| SW3 复位 | (20, 9) | 0 | |
| P1 电池焊盘 | (−7, 9.5) | 0 | |
| C1 10µF | (10, 9.5) | 0 | 顶层，避开板下电池 |

分两条道：**模块道** y −14.5…3.5 放 U2 + U1；**按键道** y 4.5…14.5 放拨动开关、三个按键、电池焊盘。元件两两净距最小 >1mm。

### 🔴 拨动开关的朝向：rot **270** 不是 90

rot 90 时拨柄朝**板内**，装上去根本拨不到。主机的 SW3 用的就是 270（拨柄尖指 −X），这次照抄错了朝向，是用户看图发现的。

⚠ **旋转 180° 会让 1/3 脚对调**（3 脚从 y=11.5 变到 7.5），所以改朝向之后**必须重布**碰到这颗开关的网络，不能只转不管走线。

拨柄尖约在元件原点外 9.4mm → 绝对 x ≈ −32.5。外壳外壁面按 −31.5 设计时，拨柄露出约 1mm，手指拨得动。

### 🔴 U1 朝向：Type-C 必须朝板边（rot 270）

最初放的是 rot 90（天线朝 +X 板边，射频最优），但那样 **Type-C 朝板内、正对着 TP4056 模块，插不进线**。用户指出后改成 **rot 270**：

- **Type-C 朝 +X 板边**，前面无遮挡（当时的打算是外壳 +X 端面开烧录口，改程序不用开壳）
  > ⚠ **这条后来被推翻了**：两个 Type-C 都通到电池，插错口会拿 5V 直接怼电芯。**外壳 +X 端不开孔，烧录靠拆壳拔模块**，见 §6c 末。板上朝向仍保持 rot 270 不变——理由变成"Type-C 朝板边才拔得动模块"。
- 充电口在 −X 端、烧录口在 +X 端，两头分开不会插错
- 天线端转到模块的 −X 侧（x ≈ 4.7…10.5），避让槽跟着挪到 **x 5.5…11，y −11…0**

天线转到板内本来是射频上的让步，但**模块插排母后架空 7.5mm**，离铜箔已经够远，影响不大。

⚠ **转 180° 会把用到的 5 个焊盘全部镜像到另一排**（从 y=+2.12 变成 y=−13.12），所以碰到 U1 的 5 条网络必须全部重布，不能只转朝向。

### 天线避让

**在两排焊盘之间挖了 5.5 × 11 的槽**，天线正下方无 FR4 无铜。

> 焊盘几乎占满模块全长（跨距 17.78 / 本体 22.52），**"让天线悬出板边"这条做不到**——悬出超过 2.4mm 焊盘就掉到板外了。所以改用挖槽。
> ⚠ **电池垫在板底下，别让它压在天线正下方**：电池仓要排在 x −28…+4 那一段。

### 布线

- **三条按键信号手工布顶层**，分车道走（目标 x 越靠右车道越高），零交叉
- **五条电源网络用内置自动布线器**（`autoRouting`），走底层，1.6 秒完成
- 线宽：信号 0.25mm

**DRC 零错误。** 中间修过一处：BTN_RST 的车道原在 y=5.6，距 SW3 的 3 脚焊盘只剩 0.22mm 净距，DRC 报 `TH Pad to Track`，车道下移到 5.2 后通过。

### 连通性验证（独立于 DRC）

读回全部焊盘与走线段做并查集，**8 条网络全部单连通**。

阳性对照：把探针从真实焊盘位置偏移 0.10 / 0.30 / 2.0mm，三档**全部判断开** —— 证明 0.03mm 的判据是严的。

> ⚠ 第一版判据取 0.02mm 时误报过一条（走线端点距焊盘中心 0.01mm，实际稳稳落在焊盘里）。**判据太严会制造假故障，太松会放过真断线**，所以必须配阳性对照来标定。

### 充电指示灯观察窗（rc_top）

**外壳坐标 (−18.7, +2.0)，窗 6 × 4。**（注意是外壳坐标，PCB 坐标是 (−18.7, +0.5)，差 `RC_PCB_DY = 1.5`）

位置不是量的，是**从主机那颗实测值反推**的——两边同一款 28×17 模块、同为 rot 0：

> 主机 LED (−80, 0)、U3 (−74.8, −6) → LED 相对模块中心 **(−5.2, +6)**
> 遥控 U2 在 PCB (−13.5, −5.5) → LED 在 PCB **(−18.7, +0.5)** = 外壳 **(−18.7, +2.0)**

⚠ **窗只能开 6×4，不能照抄主机的 10×6**：SW1 按键的 φ11.5 导向凸台在外壳 (−16, 10.5)，窗放大或往 +Y 挪就会啃进凸台，**凸台缺一块 → 按键帽导向跨度变短 → 按键晃**。现在窗最近点 (−15.7, 4) 到凸台中心 6.51mm，凸台半径 5.75 → 让开 **0.76mm**。自检 `t=90/91`（阳性对照用最初拟的 (−18, 5)，必须啃上）。

### 🔴 充电口开口要按插头塑料头开（2026-08-15 补，主机同病）

外壁面 −31.5 / 内壁面 −29.5 / 板边 −28.5 / U2 本体前沿（座口）−28.0 → **死区 3.5mm**，比主机的 3.27 还大。

Type-C 完全插入时**塑料头前端面基本贴住座口**，所以这 3.5mm 得由塑料头自己钻过去。原来 `RC_USB` 开 **10 × 4.5** 是照金属壳（8.34 × 2.56）开的，用户实测线材塑料头 **11 × 6.5**，过不去 → 插头顶在外壁上，金属壳只进 6.5 − 3.5 = 3.0mm，**接触不上**。

已改为 **12.5 × 8.0**。自检 `t=82/83`（阳性对照：塑料头放大到 14×9 必须被挡住）。

### 板框四角倒 R2 ✅（2026-08-15 补做）

外壳四周是 R3.4 圆角，板子却是直角 → 板角到腔壁只剩 0.34mm。**板四角倒 R2 后升到 1.0mm**（再倒大也没用，这时卡的是直边净距）。CAD 侧 `RC_PCB_CR = 2`，EDA 侧同步改完。

**改法**：读回原板框折线（`pcb_PrimitivePolyline`，图元 `fec1a069811a929e`，层 11），把四个直角各换成 **16 段离散化的 R2 圆弧**，再用 `pcb_PrimitivePolyline.modify(id, {polygon})` 原地改——不用删了重建，图元 ID 不变。

> **为什么不用真圆弧**：多边形数组支持 `ARC` 段，但文档自相矛盾——正文写 `startX startY ARC angle endX endY`，示例却是 `"ARC", 400, 220, 15`（末位才是角度）。这个 API 已经在"自动闭合"上骗过一次，不赌第二次。16 段离散化的最大偏差 **0.0024mm**，比铣边公差 ±0.2mm 低两个数量级。

**改完的验证（不只看 DRC）**：

| 查什么 | 结果 |
|---|---|
| 顶点数 / 闭合 | 85 点，首尾重合 |
| 包围盒 | 仍是 57 × 33（−28.5…28.5 × −18…15）|
| 每个顶点落在直边上或离圆心正好 R=2 | 全部命中，最大偏差 **1.7 nm**（EDA 的 mil 量化）|
| **阳性对照：原来四个直角必须消失** | 四个都查无此点 ✅ |
| 四个螺柱缺口 / 八个圆弧起止点还在 | 全在 |
| 铜箔到板边最小净距（自己算，不靠 DRC）| **0.623mm**，在缺口处；离板边最近的焊盘 1.35mm。倒角削掉的料附近没有任何铜 |
| DRC | 零错误 |

> ⚠ **DRC 改前改后都返回 `true`，这本身不构成证据**。上面那条净距是独立算出来的：把板框折线拆成 84 段，对每个焊盘（减铜箔半径）和每个走线端点（减半线宽）求点到折线的最小距离。

### ⚠ 五条 EDA API 坑（这次新踩）

1. **`pcb_MathPolygon.createPolygon` 不会自动闭合**，尽管文档写"如果首尾不重合将会自动重合"。板框少画了左边那条，**API 全程报成功、`zoomToBoardOutline()` 也返回 true**，是用户看图发现的。**必须显式把首点补回末尾。**
2. **`createPolygon` 只能做闭合形状，不能拿来画走线**（传开放路径直接抛"无法创建多边形图元"）。走线用 `pcb_PrimitiveLine.create(net, layer, x1,y1,x2,y2, width)` 一段一段建。
3. 🔴 **删走线只删 `pcb_PrimitiveLine` 会留下孤儿过孔**。重布之后 DRC 报 `Connection Error`，指着两个不在任何走线上的过孔。删线时必须**连 `pcb_PrimitiveVia` 一起清**。
4. **连通性校验必须分层**。第一版校验器不看层，把底层走线从顶层 SMD 焊盘底下穿过也算连上，**给了假通过**，是 DRC 抓到的。正确模型：节点 =（x, y, 层），同层线段连两端，**直插焊盘（层 12）和过孔才跨层**。
5. 🔴 **`dmt_Project.openProject()` 换不了工程，永远返回 `false`**（`openProject(uuid)` / `openProject(uuid, true)` / 把工程 uuid 传给 `openDocument` 全试过）。`getProjectInfo(uuid)` 倒是能跨工程读到信息，所以不是权限问题。**跨工程只能让人手点开**。同工程内换文档没问题：`dmt_EditorControl.openDocument(docUuid)` 正常，返回 `docUuid@projectUuid`。
   > 顺带：`dmt_Pcb.getAllPcbsInfo()` 只列**当前工程**的 PCB，所以工程没打开就拿不到它的文档 uuid，也就没法绕过这一步。

---

## 6c. 外壳接口与锁紧螺丝（2026-08-15 算完，板框已按此定稿）

### 三处必须露出外壳的特征

| 特征 | 面 | y | z 范围 |
|---|---|---|---|
| U2 充电 Type-C | −X | **−5.5** | 12.7 … 15.9 |
| SW4 拨柄 | −X | **+9.5** | 约 11.5 … 15.5，**拨柄尖 x = −32.5** |
| U1 烧录 Type-C | +X | **−5.5** | **19.6 … 22.8** |

三者高度差很大，因为 U1 插排母后被架空 7.5mm。

### 🔴 长度方向锁死，只能往宽度让

- **−X**：拨柄尖固定在 x=−32.5，外壁面必须落在 −31.5 左右它才露得出来。**内腔不能加长**，加长了拨柄缩回壳里。
- **+X**：USB-C 插头壳长只有 6.5mm，外壁面到座子的距离超过约 2mm 就插不到底。**同样不能加长。**

所以外壳想腾地方**只能加宽**。

### 🔴 U1 的 Type-C 顶到分模线

板面 11.1 → 排母 5.0 + 隔片 2.5 + 模块板 1.0 = 19.6 → USB-C 座顶 **22.8**，而上盖背面原本在 23.0，**开口上方只剩 0.2mm 壁**。内腔加高 1.5mm，外壳 26 → **27.5**。

### 🔴 锁紧螺柱：四个板角一个都不能用

板 57×30 装进 59×32 的内腔只剩 1mm，螺柱必须啃进板子。逐角算下来：

| 板角 | 5×5 缺口会切到 | |
|---|---|---|
| (−28.5,−15) | U2 的 6 脚焊盘 (−25.22,−12.68) | ❌ |
| (+28.5,−15) | U1 下排焊盘 (24.89,−13.12) | ❌ |
| (+28.5,+15) | SW3 焊盘 (23.05,11.25) 擦过 0.45mm | ❌ |
| (−28.5,+15) | 最近 SW4 (−23.1,13.65) | ✅ 唯一 |

**而且 −Y 边的右半段永久不可用** —— U1 那排焊盘压在 y=−13.12、x 7.11…24.89，那一整段边带动不了。

### 解法：板往 −Y 加宽 3mm 做纯安装边

**板最终 57 × 33**（x ±28.5，**y −18 … +15**）。多出来的 y −18…−15 那条是空的安装边，四个缺口开在：

| 缺口 | 位置 | 净距 |
|---|---|---|
| 下左 | x −16.5…−11.5，y −18…−15 | 离走线 10mm ✓ |
| 下右 | x 11.5…16.5，y −18…−15 | 离走线 0.86mm ✓ |
| 上左 | x −10…−5，y 12…15 | 扫描窗口 x −9…−5.5 |
| 上右 | x 8.5…13.5，y 12…15 | 扫描窗口 x 9…13 |

螺柱 φ4.4 中心落在 (−7.5, 14.2)(+11, 14.2)(−14, −17.2)(+14, −17.2)。

> ⚠ **找缺口位置必须按焊盘"铜箔边缘"算，不能按中心。** 第一版扫描用中心距 0.45mm 当判据，选出来的位置 DRC 直接报 `Board Outline to TH Pad` —— THT 焊盘半径约 0.9mm，0.87mm 的中心距实际净距是 0。改成**中心距 ≥1.5mm** 后才对。
> ⚠ 自动布线器把板边当走线通道用了（它当时看到的是光板），所以**改板框之后必须重跑 DRC**，不能假定走线还合法。

### 外壳最终尺寸 **63 × 40 × 26**

🔴 **电池实测 32 × 20 × 4.6**（不是早先文档写的含保护板 7×21×32），薄了 2.4mm。高度链重算：

| 层 | 累计 z |
|---|---|
| 地板 2 + 电池 **4.6** | 6.6 |
| 支撑 0.5 + 板 1.6 | **8.7（板面）** |
| U1 叠高 11.7 | 20.4 |
| 上盖背面 23 / 上盖 3 | **26.0** |

**为什么是 26 不是 24**：24 时上盖背面在 21，而按键帽的 φ11 导向凸台要往下伸 2mm 到 19 —— 而 U1 叠高顶面在 20.4，**凸台会撞在模块上**。凸台位置由 PCB 的按键坐标定死（SW3 在 x=20，正压在 U1 上方），挪不了，只能把腔加高。这条是自检 `t=64` 抓到的，不是算出来的。

### ❌ 不开 U1 的烧录口（2026-08-15 用户拍板）

两个 Type-C 并存的风险：**充电器插错到 C3 那个口**，5V 会顺着 5V 脚 → 拨动开关 → TP4056 的 OUT+，而 OUT+ 与 B+ 直连（保护只切负极）= 直接怼电池。DW01 能在 4.28V 兜住，但那是靠保护救命不是设计。

- **开发板本身烧不了** —— C3 的 USB 口本来就吃 5V
- 想彻底堵死要在开关后串肖特基，0.3V 压降会让电池到 3.3V 就用不了，白扔一截容量
- **改成拆壳拔模块烧录**（排母就是为此装的）

顺带：那个开口原本把外壳顶到 27.5（USB-C 座顶 22.8 差点豁穿分模线），去掉后高度只由电池和叠高决定。

### ~~外壳目标~~（已被上面取代）

板 57 × 30 → 内腔约 59 × 32 → **外壳约 63 × 36 × 26**。

高度链（插排母之后）：

| 层 | 累计 z |
|---|---|
| 地板 2 + 电池 7 | 9 |
| 支撑柱 0.5 + 板 1.6 | **11.1（板面）** |
| U1 叠高 11.7 | 22.8 |
| 留 0.2 + 上盖 3 | **26.0** |

⚠ **26mm 对手持件偏厚。** 想压薄只有一条路：**把电池从板底下挪到板旁边**，能省 7.5mm（降到约 18.5），代价是平面变长（约 89 × 30，更像细长的电视遥控）。这个取舍留到做外壳时定。

---

## 7. 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1 | 2026-08-15 | 首版。按用户三条决定（无状态灯 / 无电量检测 / 尺寸可动）定案；按键避开 strapping 脚 GPIO2；定死拨动开关在电源链上的位置（顺带解决烧录倒灌）；电池端子改焊盘（KF128 太高装不下）|
| v1.1 | 2026-08-15 | **原理图已建并校验通过**。按键最终落 **GPIO4/3/1**（原计划 3/4/5，因 GPIO5 在符号左侧会导致走线交叉）；补 EDA 选型锁定表与工程 uuid；记录两条 API 退化（网络名读不到、网络标识放不进去）|
| v1.2 | 2026-08-15 | **PCB 完成，DRC 零错误**。板 57×30；封装几何已核（U1 行距 15.24 用户实测确认）；拨动开关朝向更正为 rot 270；天线避让改用挖槽（悬出做不到）；补 §6b 全部 PCB 结论与两条新 API 坑 |
| v1.3 | 2026-08-15 | **U1 改插低背排母**（要反复拔下来改程序，封装不用动）→ 叠高 6.7→**11.7**、外壳 22→**26**、按键必须配打印帽。**U1 转 rot 270 让 Type-C 朝板边**，外壳 +X 端加烧录口，改程序不用开壳；天线避让槽随之挪到 x 5.5…11。补两条新 API 坑（孤儿过孔、连通性校验必须分层）|
| v1.4 | 2026-08-15 | **按三处开口 + 锁紧螺丝重算尺寸链**（§6c）。发现：长度方向被拨柄行程和 USB 插头深度**双向锁死**，只能加宽；U1 的 Type-C 顶到分模线 → 外壳加高到 **27.5**；**四个板角一个都用不了**、−Y 边右半段被 U1 焊盘永久占死 → 板往 −Y **加宽 3mm 做纯安装边**，板 57×30 → **57×33**。外壳定为 **63 × 40 × 27.5**。DRC 重跑零错误 |
